# مستندات فنی پروژهٔ وب‌سایت نمونه‌کار سعید موسوی

این سند کل فرآیند طراحی، ساخت، انتشار و رفع‌اشکال وب‌سایت را — از صفر تا وضعیت فعلی — به‌صورت فنی و با جزئیات ثبت می‌کند. هدف این است که هر کسی (چه توسعه‌دهنده، چه فرد فنی دیگر) با خواندن آن، کل معماری و تصمیم‌ها و مسیر اجرا را کامل بفهمد و بتواند پروژه را نگه‌داری یا بازتولید کند.

- **آدرس زندهٔ سایت:** https://saeedmoucavi.github.io/portfolio/
- **پنل ویرایش:** https://saeedmoucavi.github.io/portfolio/admin/
- **مخزن کد:** `github.com/saeedmoucavi/portfolio` (Public)
- **تاریخ تدوین:** ۱۴۰۵/۰۶ (سپتامبر ۲۰۲۶)

---

## فهرست

1. [خلاصهٔ اجرایی](#۱-خلاصهٔ-اجرایی)
2. [الزامات و تصمیم‌های کلان](#۲-الزامات-و-تصمیمهای-کلان)
3. [معماری کلی](#۳-معماری-کلی)
4. [ساختار فایل‌ها](#۴-ساختار-فایلها)
5. [مدل داده و موتور رندر](#۵-مدل-داده-و-موتور-رندر)
6. [سیستم دوزبانه و راست‌به‌چپ](#۶-سیستم-دوزبانه-و-راستبهچپ)
7. [لایهٔ طراحی (CSS)](#۷-لایهٔ-طراحی-css)
8. [پنل مدیریت محتوا (Decap CMS)](#۸-پنل-مدیریت-محتوا-decap-cms)
9. [استخراج محتوای واقعی از رزومه و گزارش‌ها](#۹-استخراج-محتوای-واقعی-از-رزومه-و-گزارشها)
10. [زیرساخت انتشار: GitHub Pages](#۱۰-زیرساخت-انتشار-github-pages)
11. [سازوکار ورود پنل: Cloudflare Worker + OAuth](#۱۱-سازوکار-ورود-پنل-cloudflare-worker--oauth)
12. [کارنامهٔ مشکلات و راه‌حل‌ها](#۱۲-کارنامهٔ-مشکلات-و-راهحلها)
13. [ابزار پیش‌نمایش محلی](#۱۳-ابزار-پیشنمایش-محلی)
14. [وضعیت فعلی و کارهای باقی‌مانده](#۱۴-وضعیت-فعلی-و-کارهای-باقیمانده)
15. [راهنمای نگه‌داری برای توسعه‌دهندهٔ آینده](#۱۵-راهنمای-نگهداری-برای-توسعهدهندهٔ-آینده)

---

## ۱. خلاصهٔ اجرایی

وب‌سایت یک **صفحهٔ تک (single-page)** استاتیک است که:

- **بدون هیچ مرحلهٔ build** ساخته می‌شود؛ فایل‌های داخل پوشهٔ `site/` مستقیماً همان چیزی هستند که منتشر می‌شود.
- **کل محتوایش** (هر متن، هر عکس، رنگ‌ها، ترتیب بخش‌ها، پروژه‌ها) در **یک فایل `content.json`** نگه‌داری می‌شود.
- یک اسکریپت مرورگری (`assets/js/main.js`) این فایل را می‌خواند و کل صفحه را در سمت کلاینت **رندر** می‌کند.
- **دوزبانه** است (انگلیسی/فارسی) با تغییر خودکار به **چیدمان راست‌به‌چپ** برای فارسی.
- روی **GitHub Pages** (میزبانی رایگان) منتشر شده و یک **پنل ویرایش تصویری** (Decap CMS) روی `/admin` دارد که با حساب گیت‌هاب و یک واسط OAuth روی **Cloudflare Workers** کار می‌کند.

پشتهٔ فناوری عمداً حداقلی است: **HTML + CSS + JavaScript خالص (Vanilla)**، بدون فریم‌ورک، بدون دیتابیس، بدون بک‌اند. دلیل این انتخاب در بخش بعد توضیح داده شده.

---

## ۲. الزامات و تصمیم‌های کلان

### الزامات کارفرما
- سایت حرفه‌ای و مدرن برای معرفی یک مهندس مکانیک (طراحی و ساخت ماشین‌آلات صنعتی).
- **قابلیت ویرایش کامل توسط فرد غیرفنی**: همهٔ متن‌ها، عکس‌ها، رنگ‌ها، ترتیب/نمایش بخش‌ها و افزودن/حذف پروژه بدون دست‌زدن به کد.
- واکنش‌گرا (دسکتاپ/تبلت/موبایل)، انیمیشن‌های ملایم، بارگذاری سریع، کد تمیز.

### تصمیم‌های کلیدی و دلایلشان

| تصمیم | گزینهٔ انتخاب‌شده | دلیل |
|---|---|---|
| مدل محتوا | `content.json` + Decap CMS | یک منبع واحد حقیقت (single source of truth) برای همهٔ محتوا؛ هم قابل ویرایش دستی، هم با پنل تصویری. جدا کردن کامل «محتوا» از «کد». |
| فریم‌ورک | هیچ (Vanilla JS) | بدون مرحلهٔ build، بدون وابستگی که کهنه شود، بدون نیاز به `npm install` برای انتشار. سایت در دو سال آینده هم بدون تعمیر کار می‌کند. |
| دوزبانگی | ساختار `{en, fa}` در همان JSON + رندر سمت کلاینت | بدون فایل ترجمهٔ جدا، بدون ساخت دو صفحهٔ مجزا؛ سوییچ زبان بدون رفرش. |
| میزبانی | GitHub Pages (به‌جای Netlify) | رایگان دائمی، از داخل ایران قابل‌دسترس‌تر، بدون نیاز به کارت بین‌المللی. (انتخاب نهایی کارفرما.) |
| پنل ادمین | Decap CMS با backend گیت‌هاب | ویرایش تصویری بدون کد، مستقیماً روی همان مخزن commit می‌کند. |
| فرم تماس | حذف شد؛ فقط ایمیل/تلفن/واتساپ | GitHub Pages نمی‌تواند فرم POST را پردازش کند؛ برای مخاطب صنعتی، تماس مستقیم مؤثرتر است. |

> **نکتهٔ تاریخی:** در ابتدا در پوشهٔ `portfolio/` یک نسخهٔ قبلی مبتنی بر **Astro** وجود داشت (با محتوای ساختگی/placeholder). این نسخه به‌درخواست کارفرما در تاریخ ۱۴۰۵/۰۶ حذف شد و کل کار در پوشهٔ جدید `site/` بازنویسی شد.

---

## ۳. معماری کلی

```
┌──────────────────────────────────────────────────────────────────┐
│                      مرورگر بازدیدکننده                            │
│                                                                    │
│   index.html  ──►  main.js  ──fetch──►  content.json               │
│      (اسکلت)      (رندر DOM)          (همهٔ محتوا)                  │
│                       │                                            │
│                       ├──► styles.css  (طراحی؛ رنگ‌ها از theme)     │
│                       └──► assets/images/*  (تصاویر)               │
└──────────────────────────────────────────────────────────────────┘
          ▲                                     ▲
          │ منتشر می‌شود از                      │ ویرایش می‌شود با
          │                                     │
┌─────────┴──────────┐              ┌───────────┴─────────────┐
│    GitHub Pages     │◄─commit──── │   Decap CMS (/admin)    │
│  (میزبانی استاتیک)  │             │  ویرایش تصویری          │
└─────────────────────┘             └───────────┬─────────────┘
                                                 │ ورود با گیت‌هاب
                                    ┌────────────┴─────────────┐
                                    │  Cloudflare Worker       │
                                    │  (واسط OAuth گیت‌هاب)     │
                                    └──────────────────────────┘
```

**جریان کاری تولید محتوا:** کاربر در `/admin` تغییر می‌دهد → Decap با توکن گیت‌هاب یک commit روی `content.json` می‌زند → GitHub Pages خودکار بازانتشار می‌کند → تا حدود یک دقیقه بعد سایت زنده به‌روز می‌شود.

---

## ۴. ساختار فایل‌ها

```
site/
├── index.html                  اسکلت صفحه + fallbackهای SEO + بارگذاری فونت‌ها
├── content.json            ★   تنها منبع محتوا: متن‌ها، عکس‌ها، رنگ‌ها، پروژه‌ها
├── favicon.svg                 آیکون تب مرورگر (چرخ‌دنده/رجیستر مارک)
├── robots.txt                  اجازهٔ ایندکس گوگل + آدرس sitemap
├── sitemap.xml                 نقشهٔ سایت برای موتور جستجو
├── .nojekyll                   غیرفعال‌کردن پردازش Jekyll در GitHub Pages
│
├── admin/                      پنل مدیریت محتوا (Decap CMS)
│   ├── index.html              بارگذار Decap
│   └── config.yml              تعریف backend + همهٔ فیلدهای قابل‌ویرایش
│
└── assets/
    ├── css/styles.css          کل طراحی؛ رنگ‌ها در زمان اجرا از content.json تزریق می‌شوند
    ├── js/main.js              موتور رندر: JSON → DOM
    ├── files/
    │   └── cv-saeed-mousavi-en.pdf     رزومه (دکمهٔ دانلود)
    └── images/                 همهٔ تصاویر در یک پوشهٔ تخت (flat)
        ├── portrait-placeholder.svg
        ├── og-image.jpg
        └── *.jpg               ۱۴ عکس/رندر پروژه‌ها

# فایل‌های خارج از site/ (منتشر نمی‌شوند):
cloudflare-oauth-worker.js      کد واسط ورود که در Cloudflare کپی می‌شود
preview-server.mjs              سرور پیش‌نمایش محلی
preview.cmd                     اجراکنندهٔ سرور پیش‌نمایش (دابل‌کلیک)
```

**فایل‌های راهنما (فارسی/انگلیسی):** `راهنمای-فارسی.md`, `راهنمای-گیت‌هاب-پیجز.md`, `README.md`, `EDITING-GUIDE.md`, `LAUNCH-GUIDE.md`, و همین `مستندات-فنی.md`.

★ = تنها فایلی که برای تغییر محتوا لازم است دست بخورد.

---

## ۵. مدل داده و موتور رندر

### ساختار `content.json`
یک شیء JSON با کلیدهای سطح‌بالا که هر کدام یک بخش سایت را توصیف می‌کنند:

```
site        تنظیمات پایه (عنوان، توضیح متا، زبان پیش‌فرض، آدرس سایت، تصویر اشتراک‌گذاری)
theme       رنگ‌ها، فونت‌ها، شعاع گوشه، روشن/خاموش‌بودن گرید و انیمیشن
sections    آرایه‌ای که «ترتیب» و «نمایش/مخفی» و «منو» هر بخش را تعیین می‌کند
hero        بالای صفحه (نام، شعار، معرفی، دکمه‌ها، عکس)
stats       چهار عدد آماری
about        متن درباره‌من + جدول مشخصات
skills      گروه‌های مهارت با درصد
projects    آرایهٔ پروژه‌ها (هر کدام: عنوان، دسته، متن‌ها، مشخصات، عکس‌ها)
experience  سوابق کاری (خط زمانی)
education   تحصیلات + دوره‌ها/انتشارات
contact     اطلاعات تماس + شبکه‌های اجتماعی + (فرم؛ اکنون خاموش)
footer      پاورقی
```

### موتور رندر (`assets/js/main.js`)
یک IIFE (تابع بلافاصله‌اجراشونده) که:

1. **`boot()`** — با `fetch('content.json')` داده را می‌گیرد. اگر JSON خراب باشد، به‌جای صفحهٔ سفید، یک پیام دوستانهٔ راهنما (با اشاره به jsonlint.com) نمایش می‌دهد.
2. **`applyTheme()`** — مقادیر `theme` را به‌صورت CSS Custom Properties روی `:root` تزریق می‌کند (مثلاً `--accent`). مقادیر داخل `styles.css` فقط fallback هستند.
3. **`applyLanguage()`** — زبان فعال را تعیین، `<html lang>` و `<html dir>` را ست، و انتخاب کاربر را در `localStorage` ذخیره می‌کند.
4. **`render()`** — بر اساس آرایهٔ `sections` (با ترتیب و فیلتر `enabled`)، تابع رندر هر بخش را صدا می‌زند و رشتهٔ HTML نهایی را در `#app` می‌گذارد.
5. **`wireUp()`** — رفتارها را وصل می‌کند: ناوبری، مشاهدهٔ ورود بخش‌ها (IntersectionObserver برای انیمیشن)، مودال پروژه‌ها + گالری، شمارش اعداد، و **JSON-LD ساختاریافته** برای سئو.

نکات مهندسی:
- **امنیت XSS:** هر متن قبل از قرار گرفتن در DOM با تابع `esc()` فرار (escape) داده می‌شود، پس یک `<` یا `&` در محتوا نمی‌تواند مارک‌آپ تزریق کند.
- **آیکون‌ها** به‌صورت SVG درون‌خطی در خود `main.js` تعریف شده‌اند (بدون فایل آیکون؛ انتخاب با نام).
- **انیمیشن‌ها** با IntersectionObserver اجرا می‌شوند و برای کاربرانی که `prefers-reduced-motion` دارند یا وقتی `theme.enableAnimations=false` است، خودکار خاموش می‌شوند.
- **دسترس‌پذیری:** skip-link، `aria-*` روی مودال و منو، مدیریت فوکوس هنگام باز/بسته‌شدن مودال، پشتیبانی کیبورد (Esc و کلیدهای جهت در گالری).

---

## ۶. سیستم دوزبانه و راست‌به‌چپ

هر رشتهٔ متنی در `content.json` دو نسخه دارد:

```json
"headline": {
  "en": "I design and build the equipment that keeps a production line running.",
  "fa": "تجهیزاتی را طراحی و می‌سازم که خط تولید را سرِ پا نگه می‌دارد."
}
```

- تابع `t(value)` در `main.js` بر اساس زبان فعال نسخهٔ درست را برمی‌گرداند و با رشتهٔ ساده هم سازگار است.
- سوییچ زبان (`EN / فا`) تنها `LANG` را عوض و کل صفحه را دوباره `render()` می‌کند.
- برای فارسی، `<html dir="rtl">` ست می‌شود و **کل چیدمان با CSS Logical Properties** (مثل `inset-inline-start`, `padding-inline`, `margin-inline`) خودکار آینه می‌شود — بدون استایل‌شیت جدا برای RTL.
- فونت فارسی **Vazirmatn** است که در `styles.css` تحت `html[lang="fa"]` جایگزین فونت‌های لاتین می‌شود.
- انتخاب زبان کاربر در `localStorage` می‌ماند؛ زبان پیش‌فرض از `site.defaultLanguage` می‌آید (اکنون `fa`).

---

## ۷. لایهٔ طراحی (CSS)

هویت بصری **«Precision Steel»** (فولاد دقیق): پس‌زمینهٔ گرافیت تیره، لهجهٔ رنگی **کهربایی** (`#F2A33C`)، و خط‌های **آبی نقشه‌کشی (blueprint)** با موتیف‌های فنی.

- **توکن‌های رنگ** به‌صورت CSS Variables روی `:root` تعریف و در زمان اجرا از `content.json → theme` بازنویسی می‌شوند.
- **موتیف‌های صنعتی:** گرید کم‌رنگ blueprint در پس‌زمینهٔ بخش‌ها، تیک‌های رجیستر گوشهٔ کارت‌ها، خطوط اندازه‌گذاری (dimension lines)، شماره‌های پروژه با فونت مونواسپیس (`PRJ-01`).
- **تایپوگرافی:** `Space Grotesk` (تیترها)، `Inter` (متن)، `JetBrains Mono` (برچسب‌های فنی)، `Vazirmatn` (فارسی) — از Google Fonts.
- **واکنش‌گرایی:** breakpointها در ۹۸۰px، ۹۰۰px (تبدیل منو به همبرگری)، ۷۶۰px، ۴۶۰px. گرید پروژه‌ها با `auto-fill/minmax`.
- **حالت چاپ:** استایل `@media print` برای تولید PDF/کاغذ تمیز.

---

## ۸. پنل مدیریت محتوا (Decap CMS)

- **`admin/index.html`** فقط اسکریپت Decap را بارگذاری می‌کند (نسخهٔ `decap-cms@^3.8.4` از unpkg).
- **`admin/config.yml`** تعیین می‌کند پنل چه فیلدهایی نشان دهد. کل محتوا در **یک collection با یک file entry** روی `content.json` تعریف شده، با پنل‌های تاشو (collapsible objects) برای هر بخش.

> **تصمیم مهم — چرا یک entry به‌جای چند تا:** در پیاده‌سازی اول، هر بخش یک file-entry جدا روی `content.json` بود. مشکل: Decap هنگام ذخیره، **کل فایل را از روی فیلدهای همان entry بازنویسی می‌کند** و بقیهٔ بخش‌ها را حذف می‌کند. راه‌حل: تجمیع همه در یک entry با پنل‌های تاشو. با اسکریپت اعتبارسنجی تأیید شد که مجموعهٔ کلیدهای CMS دقیقاً برابر کلیدهای `content.json` است (هیچ بخشی از قلم نمی‌افتد).

پیکربندی backend (نسخهٔ GitHub Pages):

```yaml
backend:
  name: github
  repo: saeedmoucavi/portfolio
  branch: main
  base_url: https://portfolio-login.saeedmoucavi.workers.dev
```

- `media_folder: assets/images` و `public_folder: assets/images` — محل ذخیرهٔ عکس‌های آپلودی و مسیری که در `content.json` نوشته می‌شود.

---

## ۹. استخراج محتوای واقعی از رزومه و گزارش‌ها

محتوای واقعی از سه فایل کارفرما استخراج شد:

| منبع | ابزار | خروجی |
|---|---|---|
| رزومهٔ PDF | `pdftotext -layout -enc UTF-8` | سوابق، تحصیلات، مهارت‌ها، تماس، متن «درباره من» |
| دو گزارش عملکرد `.pptx` (۱۴۰۴ و شش‌ماههٔ ۱۴۰۵) | اسکریپت پایتون با `zipfile` + `xml.etree` (خواندن `ppt/slides/slideN.xml`) | متن ۹ پروژهٔ واقعی (نیازمندی/توصیف/سودمندی) |
| تصاویر داخل همان `.pptx`ها | پایتون `zipfile` (استخراج `ppt/media/*`) + نگاشت به اسلایدها از `slideN.xml.rels` | ۱۴ رندر CAD و عکس دستگاه ساخته‌شده |

- تصاویر با کتابخانهٔ **`sharp`** بهینه شدند: `resize` به حداکثر ۱۶۰۰px، `flatten` روی پس‌زمینهٔ سفید، `jpeg` با کیفیت ~۸۰ و `mozjpeg`. نتیجه: همهٔ فایل‌ها زیر ~۱۹۰KB.
- `content.json` با ۹ پروژهٔ واقعی (دوزبانه)، سوابق، تحصیلات، مهارت‌ها با درصد، اعداد آماری و اطلاعات تماس واقعی بازنویسی شد و `defaultLanguage` روی `fa` تنظیم شد.

> **ملاحظهٔ محرمانگی (ثبت‌شده):** محتوای پروژه‌ها از گزارش داخلی کارفرما (MAAD ARIA VISTA) است و شامل عکس سالن تولید و نام مدل‌های خودرو بوده. نام مدل‌ها از متن‌ها حذف شد. چون مخزن GitHub Pages رایگان باید Public باشد، این نکته به کارفرما گوشزد شد تا پیش از انتشار عمومی از مجاز بودنش مطمئن شود.

---

## ۱۰. زیرساخت انتشار: GitHub Pages

- **مخزن:** `saeedmoucavi/portfolio`، نوع **Public** (شرط رایگان‌بودن Pages).
- **انتشار:** Settings → Pages → Deploy from a branch → `main` / `(root)`.
- **مسیرها:** همهٔ ارجاع‌ها در HTML/CSS/JS **نسبی** هستند تا سایت زیر زیرمسیر `.../portfolio/` هم کار کند. تنها آدرس‌های مطلق، `canonical` و `sitemap`/`robots`/`siteUrl` هستند که با آدرس واقعی پر شدند:
  - `content.json → site.siteUrl` = `https://saeedmoucavi.github.io/portfolio`
  - `index.html → <link rel="canonical">` = آدرس کامل صفحه
- **`.nojekyll`** اضافه شد تا GitHub فایل‌ها را همان‌طور که هستند سرو کند (بدون پردازش Jekyll).
- **HTTPS** به‌صورت خودکار توسط GitHub Pages فعال است.

---

## ۱۱. سازوکار ورود پنل: Cloudflare Worker + OAuth

چون GitHub Pages بک‌اند ندارد، Decap برای ورود به یک «واسط OAuth» نیاز دارد که تبادل امن توکن را انجام دهد. این واسط یک **Cloudflare Worker** رایگان است.

### اجزا
- **GitHub OAuth App** («Portfolio Admin») → `Client ID` + `Client Secret`.
- **Cloudflare Worker** (`portfolio-login.saeedmoucavi.workers.dev`) با دو متغیر محرمانه: `GITHUB_CLIENT_ID` و `GITHUB_CLIENT_SECRET`.
  - **راز (Client Secret) هرگز در کد یا مخزن قرار نگرفت**؛ فقط کارفرما آن را مستقیم در داشبورد Cloudflare وارد کرد.
- **Callback URL** در OAuth App = `https://portfolio-login.saeedmoucavi.workers.dev/callback`.

### مسیرهای Worker
- `GET /auth` → کاربر را با `client_id`, `redirect_uri`, `scope=repo,user`, `state` به صفحهٔ authorize گیت‌هاب ری‌دایرکت می‌کند.
- `GET /callback` → کد یک‌بارمصرف گیت‌هاب را با `client_id`+`client_secret` به `access_token` تبدیل می‌کند و یک صفحهٔ کوچک برمی‌گرداند که توکن را به پنجرهٔ پنل تحویل می‌دهد.

### دست‌دادن (handshake) با Decap ۳.۸ — پروتکل postMessage
پروتکل دقیق از خود باندل Decap استخراج شد:

```js
// سمت Decap (پنجرهٔ ادمین):
// ۱) منتظر پیام "authorizing:github" از پاپ‌آپ می‌ماند و origin را با base_url چک می‌کند
// ۲) یک ack پس می‌فرستد و سپس منتظر نتیجه می‌ماند
if (r.data === "authorizing:github" && r.origin === base_url) { authWindow.postMessage(r.data, r.origin) }
// ۳) نتیجه باید این شکل باشد و origin آن برابر base_url باشد:
"authorization:github:success:" + JSON.stringify({ token, provider: "github" })
```

نکتهٔ حیاتی: Decap **`r.origin === base_url`** را بررسی می‌کند؛ پس `base_url` در `config.yml` باید **دقیقاً برابر origin ورکر و بدون اسلش انتهایی** باشد.

---

## ۱۲. کارنامهٔ مشکلات و راه‌حل‌ها

این بخش، مشکلات واقعی حین اجرا و نحوهٔ رفعشان را ثبت می‌کند (بخش «با جزئیات» درخواستی).

### ۱۲.۱ بازنویسی کامل فایل توسط چند entry در Decap
- **نشانه:** ذخیرهٔ هر بخش، بقیهٔ بخش‌ها را از `content.json` پاک می‌کرد.
- **علت:** هر بخش یک file-entry جدا روی همان فایل بود.
- **رفع:** تجمیع همه در یک entry با پنل‌های تاشو + اسکریپت تأیید برابری کلیدها.

### ۱۲.۲ باگ مسیر ویندوز در سرور پیش‌نمایش
- **نشانه:** صفحهٔ 404 در پیش‌نمایش محلی.
- **علت:** فاصلهٔ داخل مسیر «Cloude Code» در تبدیل `import.meta.url` به مسیر فایل، درصدی‌انکد می‌شد.
- **رفع:** استفاده از `fileURLToPath(new URL('./site/', import.meta.url))`.

### ۱۲.۳ نبودن Node در PATH
- **نشانه:** دابل‌کلیک `preview.cmd` پنجره را باز و بلافاصله می‌بست.
- **علت:** دستور `node` روی PATH سیستم کاربر نبود.
- **رفع:** ست‌کردن مسیر نصب WinGet نود در `preview.cmd` + پیام خطای واضح و نگه‌داشتن پنجره باز.

### ۱۲.۴ عکس‌های اسکرین‌شات و آپلود ناقص پوشه‌ها
- **نشانه:** اولین بار پس از آپلود، صفحهٔ زندهٔ سایت خالی بود.
- **تشخیص:** با `fetch` مشخص شد فایل‌های سطح‌اول 200 ولی همهٔ مسیرهای داخل `assets/` و `admin/` **404** هستند؛ با GitHub Contents API تأیید شد پوشه‌ها اصلاً آپلود نشده‌اند.
- **علت:** آپلودگر وب گیت‌هاب هنگام کشیدن با `Ctrl+A`، فقط فایل‌های تکی را فرستاده و زیرپوشه‌ها را جا انداخته بود.
- **رفع:** آپلود جداگانهٔ هر پوشه (`assets` و `admin`) با کشیدن خودِ پوشه.

### ۱۲.۵ گیرکردن ورود پنل (مهم‌ترین)
- **نشانه:** پس از Authorize، پاپ‌آپ می‌نوشت «Login complete» ولی پنجرهٔ ادمین روی «Login with GitHub» می‌ماند.
- **تشخیص:** ابتدا COOP بررسی شد (رد شد)، سپس پروتکل دست‌دادن دقیق از باندل Decap استخراج شد. مشخص شد ورکر پیام `authorizing:github` را فقط **یک بار** می‌فرستد و اگر همان یک بار به‌خاطر تایمینگ از دست برود، Decap ack نمی‌فرستد و ورکر برای همیشه منتظر می‌ماند.
- **رفع (ورکر نسخهٔ ۲):** ارسال پیام `authorizing:github` **هر ۲۵۰ میلی‌ثانیه** تا دریافت ack؛ به‌علاوهٔ پیام تشخیصی در صورت خالی‌بودن `window.opener`. پس از این، ورود موفق شد.

### ۱۲.۶ نبودن عکس پروژه‌ها در کتابخانهٔ Media
- **نشانه:** در بخش Media پنل فقط ۲ عکس دیده می‌شد.
- **علت:** کتابخانهٔ Media در Decap **تخت** است و زیرپوشه‌ها را نشان نمی‌دهد؛ ۱۴ عکس پروژه در زیرپوشهٔ `assets/images/projects/` بودند.
- **رفع دو بخشی:**
  1. در `config.yml`، فیلدهای `cover` و `gallery` به **بالای هر پروژه** (زیر عنوان) منتقل شدند تا راحت پیدا شوند.
  2. همهٔ ۱۴ عکس از `assets/images/projects/` به `assets/images/` منتقل شدند (ساختار تخت) و ۲۳ مسیر در `content.json` به‌روز شد؛ تا کتابخانهٔ Media همهٔ ۱۶ تصویر را یکجا نشان دهد.

---

## ۱۳. ابزار پیش‌نمایش محلی

چون سایت `content.json` را با `fetch` می‌خواند، باز کردن مستقیم `index.html` با پروتکل `file://` توسط مرورگر بلاک می‌شود. برای همین یک سرور محلی سبک ساخته شد:

- **`preview-server.mjs`** — سرور HTTP خالص Node (بدون وابستگی) که پوشهٔ `site/` را روی `http://localhost:4321` سرو می‌کند، با MIME-typeهای درست و `Cache-Control: no-store`.
- **`preview.cmd`** — با دابل‌کلیک سرور را اجرا می‌کند؛ مسیر نود را fallback می‌کند و اگر نود نبود پیام واضح می‌دهد.

---

## ۱۴. وضعیت فعلی و کارهای باقی‌مانده

**کارکرد کامل و تأییدشده:**
- سایت زنده روی GitHub Pages، فارسیِ راست‌به‌چپ به‌صورت پیش‌فرض، ۹ پروژه با تصاویر، صفر خطای کنسول.
- پنل `/admin` با ورود گیت‌هاب کار می‌کند (ورود موفق تأیید شد).
- تخت‌سازی تصاویر در کد انجام و به‌صورت محلی تست شده است.

**کارهای باقی‌مانده برای کارفرما (از طریق همان پنل `/admin`):**
- افزودن **عکس پرسنلی** واقعی (اکنون placeholder).
- افزودن **آدرس LinkedIn** واقعی.
- نوشتن **شرح شغل تلاش B.S.R** (فعلاً خالی).
- بازبینی اعداد آماری و درصد مهارت‌ها (از روی گزارش‌ها استخراج شده‌اند).
- **پاک‌سازی اختیاری:** پوشهٔ orphan `assets/images/projects/` و فایل زائد `nojekyll.` در مخزن (بی‌ضرر).

---

## ۱۵. راهنمای نگه‌داری برای توسعه‌دهندهٔ آینده

- **تغییر محتوا:** فقط `content.json` (دستی یا از `/admin`). هر متن دو کلید `en`/`fa` دارد.
- **افزودن بخش جدید:** یک رندرکنندهٔ جدید در `main.js` (شیء `RENDERERS`) بنویس، یک ورودی در آرایهٔ `sections` اضافه کن، و فیلدهایش را در `admin/config.yml` تعریف کن.
- **رنگ/فونت:** از `content.json → theme` (فونت جدید باید به لینک Google Fonts در `index.html` هم اضافه شود).
- **مسیرها همیشه نسبی بمانند** تا سایت زیر زیرمسیر GitHub Pages کار کند.
- **پس از هر تغییر `config.yml`،** فایل باید در گیت‌هاب دوباره آپلود/کامیت شود تا پنل به‌روز شود.
- **ورکر OAuth:** کد در `cloudflare-oauth-worker.js`؛ اگر `base_url` عوض شد، باید هم در `config.yml` و هم در Callback URL اپ گیت‌هاب هماهنگ شود (بدون اسلش انتهایی).
- **اعتبارسنجی:** `content.json` را با `node -e "require('./content.json')"` یا jsonlint.com و `config.yml` را با یک YAML linter بررسی کن.

---

*پایان سند. برای راهنمای گام‌به‌گام غیرفنی، به `راهنمای-فارسی.md` و `راهنمای-گیت‌هاب-پیجز.md` مراجعه کنید.*
