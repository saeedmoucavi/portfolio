# راهنمای کامل سایت شما

سلام سعید 👋

این یک راهنمای کامل است برای اینکه سایتتان را **خودتان** بالا بیاورید و **خودتان** ویرایش کنید — بدون اینکه لازم باشد حتی یک خط کد بلد باشید.

راهنما را از اول تا آخر لازم نیست یکجا بخوانید. هر وقت کاری داشتید، سراغ همان بخش بروید.

---

## فهرست

**بخش اول — آشنایی**
1. [این سایت چیست و چطور کار می‌کند](#۱-این-سایت-چیست-و-چطور-کار-میکند)
2. [چطور سایت را روی کامپیوتر خودتان ببینید](#۲-چطور-سایت-را-روی-کامپیوتر-خودتان-ببینید)
3. [هر فایل و پوشه چه کاری می‌کند](#۳-هر-فایل-و-پوشه-چه-کاری-میکند)

**بخش دوم — ویرایش سایت**
4. [دو راه برای ویرایش](#۴-دو-راه-برای-ویرایش)
5. [قانون‌های طلایی ویرایش دستی](#۵-قانونهای-طلایی-ویرایش-دستی)
6. [بخش به بخش: چه چیزی کجاست](#۶-بخش-به-بخش-چه-چیزی-کجاست)
7. [پروژه‌ها: اضافه کردن، حذف کردن، مرتب کردن](#۷-پروژهها-اضافه-کردن-حذف-کردن-مرتب-کردن)
8. [عکس‌ها و عکس پرسنلی](#۸-عکسها-و-عکس-پرسنلی)
9. [رنگ‌ها و فونت‌ها](#۹-رنگها-و-فونتها)
10. [جابه‌جا کردن و مخفی کردن بخش‌ها](#۱۰-جابهجا-کردن-و-مخفی-کردن-بخشها)
11. [رزومه](#۱۱-رزومه)

**بخش سوم — بردن سایت روی اینترنت**
12. [قبل از شروع: دو راه پیش رویتان است](#۱۲-قبل-از-شروع-دو-راه-پیش-رویتان-است)
13. [قدم ۱ — خریدن دامنه](#قدم-۱--خریدن-دامنه)
14. [قدم ۲ — انتخاب هاست](#قدم-۲--انتخاب-هاست)
15. [قدم ۳ — گذاشتن فایل‌ها روی گیت‌هاب](#قدم-۳--گذاشتن-فایلها-روی-گیتهاب)
16. [قدم ۴ — انتشار با Netlify](#قدم-۴--انتشار-با-netlify)
17. [قدم ۵ — وصل کردن دامنه](#قدم-۵--وصل-کردن-دامنه)
18. [قدم ۶ — روشن کردن قفل سبز (HTTPS)](#قدم-۶--روشن-کردن-قفل-سبز-https)
19. [قدم ۷ — روشن کردن پنل ویرایش](#قدم-۷--روشن-کردن-پنل-ویرایش)
20. [قدم ۸ — پیام‌های فرم تماس](#قدم-۸--پیامهای-فرم-تماس)
21. [راه ایران — اگر Netlify جواب نداد](#۲۱-راه-ایران--اگر-netlify-جواب-نداد)

**بخش چهارم — بعد از انتشار**
22. [دیده شدن در گوگل (SEO)](#۲۲-دیده-شدن-در-گوگل-seo)
23. [تست نهایی قبل از اینکه به کسی نشان بدهید](#۲۳-تست-نهایی-قبل-از-اینکه-به-کسی-نشان-بدهید)
24. [وقتی چیزی خراب شد](#۲۴-وقتی-چیزی-خراب-شد)
25. [⚠️ کارهایی که حتماً باید خودتان انجام دهید](#۲۵-️-کارهایی-که-حتماً-باید-خودتان-انجام-دهید)

---

# بخش اول — آشنایی

## ۱. این سایت چیست و چطور کار می‌کند

سایت شما یک سایت **استاتیک** است. یعنی چه؟ یعنی سایت شما فقط چند تا فایل ساده است — نه دیتابیسی دارد، نه سروری که مدام باید کار کند، نه چیزی که هک شود یا خراب شود.

ایدهٔ اصلی خیلی ساده است:

```
       content.json                    main.js                  سایت شما
   (همهٔ متن‌ها و عکس‌ها)   ────────►  (می‌خواند و می‌سازد)  ────────►  ✅
```

**تمام چیزی که در سایت می‌بینید — هر کلمه، هر رنگ، هر عکس، هر پروژه — داخل یک فایل است به اسم `content.json`.**

پس:
- می‌خواهید متنی را عوض کنید؟ `content.json` را عوض می‌کنید.
- می‌خواهید پروژه‌ای اضافه کنید؟ `content.json`.
- می‌خواهید رنگ نارنجی سایت را آبی کنید؟ باز هم `content.json`.

و برای اینکه لازم نباشد با فایل سر و کله بزنید، یک **پنل ویرایش تصویری** هم برایتان گذاشته‌ام که بعداً روی آدرس `سایت‌شما.com/admin` باز می‌شود و همه‌چیز آنجا به‌صورت جعبه و دکمه است.

### سایت دو زبانه است

هر متنی در سایت دو نسخه دارد: `en` (انگلیسی) و `fa` (فارسی). مثلاً:

```json
"headline": {
  "en": "I design and build the equipment that keeps a production line running.",
  "fa": "تجهیزاتی را طراحی و می‌سازم که خط تولید را سرِ پا نگه می‌دارد."
}
```

بازدیدکننده با دکمهٔ **EN / فا** گوشهٔ بالای سایت زبان را عوض می‌کند. وقتی فارسی انتخاب می‌شود، **کل سایت خودبه‌خود راست‌چین می‌شود** و فونت فارسی می‌آید. لازم نیست شما کاری بکنید.

سایت به‌صورت پیش‌فرض **فارسی** باز می‌شود. اگر می‌خواهید انگلیسی باز شود، در پنل ادمین قسمت «تنظیمات پایه» گزینهٔ «زبان اولی که نمایش داده می‌شود» را روی `en` بگذارید.

---

## ۲. چطور سایت را روی کامپیوتر خودتان ببینید

روی فایل **`preview.cmd`** دوبار کلیک کنید (این فایل یک پوشه بالاتر از پوشهٔ `site` است، کنارش).

یک پنجرهٔ مشکی باز می‌شود و می‌نویسد `Preview running`. حالا مرورگرتان را باز کنید و بروید به:

```
http://localhost:4321
```

سایتتان را می‌بینید. تا وقتی دارید کار می‌کنید آن پنجرهٔ مشکی را باز بگذارید؛ آخر کار ببندیدش.

> **چرا نمی‌شود فقط روی `index.html` دوبار کلیک کرد؟**
> چون سایت متن‌هایش را از `content.json` می‌خواند، و مرورگرها به دلایل امنیتی اجازه نمی‌دهند یک صفحه که مستقیم از روی هارد باز شده فایل دیگری را بخواند. پنجرهٔ پیش‌نمایش دقیقاً همین مشکل را حل می‌کند.

**نکتهٔ مهم:** بعد از هر تغییر، در مرورگر `Ctrl + F5` بزنید تا نسخهٔ جدید بارگذاری شود (نه `F5` تنها).

---

## ۳. هر فایل و پوشه چه کاری می‌کند

```
site/
├── content.json     ⭐ همهٔ متن‌ها، عکس‌ها، رنگ‌ها و پروژه‌ها. تنها فایلی که کار دارید.
├── index.html          اسکلت صفحه. تقریباً هیچ‌وقت دست نمی‌زنید.
├── favicon.svg         آیکون کوچک بالای تب مرورگر.
├── robots.txt          به گوگل می‌گوید اجازه دارد سایت را ببیند.
├── sitemap.xml         نقشهٔ سایت برای گوگل.
├── netlify.toml        تنظیمات شرکت هاست. دست نزنید.
│
├── admin/              پنل ویرایش تصویری (آدرس /admin)
│   ├── index.html      پنل را باز می‌کند.
│   └── config.yml      تعیین می‌کند چه جعبه‌هایی در پنل ببینید.
│
└── assets/
    ├── css/styles.css      کل طراحی و ظاهر سایت. رنگ‌ها از content.json می‌آید.
    ├── js/main.js          content.json را می‌خواند و صفحه را می‌سازد. دست نزنید.
    ├── files/
    │   └── cv-saeed-mousavi-en.pdf     رزومهٔ شما (دکمهٔ «دانلود رزومه»).
    └── images/
        ├── portrait-placeholder.svg    ⚠️ جای عکس پرسنلی شما — باید عوض شود.
        ├── og-image.jpg                عکسی که موقع اشتراک‌گذاری در واتساپ نشان داده می‌شود.
        └── projects/                   عکس‌های پروژه‌ها.
```

⭐ = تنها فایلی که معمولاً لازم است تغییرش دهید.

**فایل‌های راهنما:**
- `راهنمای-فارسی.md` — همین فایلی که دارید می‌خوانید (کامل‌ترین)
- `README.md` — توضیح ساختار فایل‌ها (انگلیسی)
- `EDITING-GUIDE.md` — راهنمای ویرایش (انگلیسی)
- `LAUNCH-GUIDE.md` — راهنمای انتشار (انگلیسی)

---

# بخش دوم — ویرایش سایت

## ۴. دو راه برای ویرایش

### راه اول — پنل تصویری (این را توصیه می‌کنم)

بعد از اینکه سایت روی اینترنت رفت (بخش سوم)، بروید به:

```
https://سایت-شما.com/admin
```

وارد می‌شوید و یک محیط کاملاً معمولی می‌بینید: جعبهٔ متن، دکمهٔ آپلود عکس، انتخابگر رنگ، و دکمهٔ **+** برای اضافه کردن پروژه. تغییر می‌دهید، دکمهٔ **Publish** را می‌زنید، و حدود یک دقیقه بعد سایت خودش به‌روز می‌شود.

**این روش عادی کار کردن شماست.** هیچ کدی، هیچ ریسکی.

### راه دوم — ویرایش مستقیم فایل

فایل `site/content.json` را با **Notepad** ویندوز (یا هر ویرایشگر متن) باز کنید، متن‌های داخل گیومه را عوض کنید، ذخیره کنید، و در پیش‌نمایش `Ctrl + F5` بزنید.

این روش وقتی خوب است که می‌خواهید سریع کلی تغییر بدهید، یا هنوز سایت روی اینترنت نرفته.

---

## ۵. قانون‌های طلایی ویرایش دستی

اگر تصمیم گرفتید مستقیم `content.json` را دست بزنید، فقط این چند تا قانون را رعایت کنید:

### قانون ۱ — فقط متن داخل گیومه را عوض کنید

```
  "headline": {
    "fa": "تجهیزاتی را طراحی و می‌سازم که خط تولید را سرِ پا نگه می‌دارد.",
             ↑ ــــــــــــــ فقط این قسمت را عوض کنید ــــــــــــــ ↑
```

### قانون ۲ — این علامت‌ها را پاک نکنید

| علامت | کارش چیست |
|---|---|
| `"` | ابتدا و انتهای متن را مشخص می‌کند. همیشه جفتی است. |
| `,` | یک مورد را از مورد بعدی جدا می‌کند. |
| `{ }` | چند چیز را با هم گروه می‌کند. |
| `[ ]` | یک لیست را مشخص می‌کند. |

### قانون ۳ — اگر متنتان گیومه دارد

اگر خواستید داخل متن گیومه بگذارید، به جای `"` بنویسید `\"`.
مثال: `"او گفت \"بله\" و رفت."`

### قانون ۴ — هر دو زبان را پر کنید

اگر `fa` را پر کنید و `en` را خالی بگذارید، بازدیدکنندهٔ انگلیسی متن فارسی را می‌بیند — کار می‌کند ولی نصفه‌کاره به نظر می‌رسد.

### قانون ۵ — بعد از ویرایش، فایل را چک کنید

بروید به [jsonlint.com](https://jsonlint.com) → کل محتوای فایل را کپی و پیست کنید → دکمهٔ **Validate** را بزنید.
سبز شد یعنی سالم است. قرمز شد، دقیقاً می‌گوید کدام خط مشکل دارد.

### قانون ۶ — قبل از تغییر بزرگ، یک کپی بگیرید

از `content.json` یک کپی بگیرید و اسمش را بگذارید `content-backup.json`. اگر خراب شد، برمی‌گردید سر جای اول.

---

## ۶. بخش به بخش: چه چیزی کجاست

### 🔹 بالای صفحه (Hero)

> صفحهٔ اول با اسم بزرگ و عکستان.

**در پنل:** پنل «Top of the page» — **در فایل:** دنبال `"hero"` بگردید

| چیزی که در سایت می‌بینید | اسمش در فایل |
|---|---|
| خط نارنجی کوچک بالای اسم | `eyebrow` |
| اسم بزرگ شما | `name` |
| جملهٔ نارنجی بزرگ | `headline` |
| پاراگراف خاکستری زیرش | `intro` |
| متن دکمهٔ نارنجی | `primaryButtonLabel` |
| متن دکمهٔ دوم | `secondaryButtonLabel` |
| فایل رزومه | `secondaryButtonLink` |
| **عکس پرسنلی شما** | `photo` |
| نوشتهٔ کوچک زیر عکس | `photoCaption` |

### 🔹 چهار عدد بزرگ (Stats)

**در پنل:** «Headline numbers» — **در فایل:** `"stats"`

الان این اعداد را گذاشته‌ام:

| عدد | معنی | از کجا آمده |
|---|---|---|
| ۵+ | سال طراحی مکانیکی | از ۱۴۰۰ که در آزمایشگاه آرَس شروع کردید تا الان |
| ۳۰+ | پروژهٔ تجهیزاتی تحویل‌شده | ۲۵ پروژهٔ آهنگری + ۸ پروژهٔ مهندسی گزارش ۱۴۰۴ |
| ۲۹۰+ | دستگاه و تجهیز ساخته‌شده | جمع تعدادهای جدول گزارش ۱۴۰۴ |
| ۶ | سالن تولید تجهیزشده | ساراول، مونتاژ، فوم، خیاطی، غذاخوری، کارگاه مهندسی |

⚠️ **این اعداد را حتماً خودتان تأیید یا اصلاح کنید.** من از روی گزارش‌هایتان حساب کردم، ولی شما بهتر می‌دانید. اگر عددی درست نیست، عوضش کنید. اگر اصلاً نمی‌خواهید این نوار باشد، در پنل تیک «Show this strip» را بردارید (یا در فایل `"enabled": false` بگذارید).

اعداد وقتی بازدیدکننده به آنها می‌رسد، از صفر تا عدد نهایی بالا می‌روند.

### 🔹 درباره من

**در پنل:** «About me» — **در فایل:** `"about"`

- `heading` — جملهٔ بزرگ («ماشین در سالن تولید قضاوت می‌شود، نه در پنجرهٔ نرم‌افزار.»)
- `paragraphs` — سه پاراگراف بیوگرافی. هر `{ "en": ..., "fa": ... }` یک پاراگراف است.
  اینها را از متن «About Me» رزومه‌تان و گزارش‌های عملکردتان نوشته‌ام. **حتماً بخوانیدشان** و اگر جایی حرف من با حرف شما فرق دارد، عوضش کنید.
- `specs` — جدول کوچک کنار متن (محل استقرار، تجربه، حوزهٔ تمرکز و…)

**برای اضافه کردن پاراگراف چهارم:** کل بلوک `{ "en": ..., "fa": ... }` را کپی کنید، بعد از آخری پیست کنید، و بینشان یک `,` بگذارید.

### 🔹 مهارت‌ها

**در پنل:** «Skills» — **در فایل:** `"skills"`

چهار گروه دارید:
1. **طراحی و نقشه‌کشی** — SolidWorks، CATIA، AutoCAD، طراحی فیکسچر، GD&T
2. **ساخت و تولید** — طراحی برای ساخت، سازهٔ جوشی، پروفیل آلومینیوم، نظارت بر ساخت
3. **اتوماسیون و مکاترونیک** — پنوماتیک، مکانیزم و سینماتیک، رباتیک، کنترل هوشمند
4. **تحلیل و برنامه‌نویسی** — MATLAB، Python، یادگیری ماشین، پردازش تصویر و سیگنال

هر مهارت یک `level` بین ۰ تا ۱۰۰ دارد که همان نوار نارنجی کوچک است.

⚠️ **درصدها را من از روی رزومه‌تان گذاشتم:** هر چیزی که در رزومه Advanced بود ۹۰، Intermediate بود ۶۵. بقیه را از روی پروژه‌هایتان حدس زدم. **حتماً یک بار مرورشان کنید** — اینها ادعای شماست، نه من.

راهنمای واقع‌بینانه برای عددگذاری: ۹۰ یعنی می‌توانید به دیگران یاد بدهید، ۷۵ یعنی هر روز با اطمینان کار می‌کنید، ۵۰ یعنی بلدید، ۳۰ یعنی مقدماتی.

> اگر اصلاً دوست ندارید به خودتان نمره بدهید، بگویید تا نوارها را بردارم و فقط لیست اسم‌ها بماند. خیلی‌ها این را ترجیح می‌دهند.

### 🔹 سوابق کاری

**در پنل:** «Work experience» — **در فایل:** `"experience"`

سه شغل از رزومه‌تان وارد شده:
1. **ماد آریا ویستا** — تیر ۱۴۰۴ تا کنون (با ۴ بند دستاورد که از گزارش‌هایتان نوشتم)
2. **تلاش B.S.R** — آبان ۱۴۰۳ تا تیر ۱۴۰۴ ⚠️ **این یکی خالی است** — رزومه‌تان توضیحی برایش نداشت. باید خودتان دو سه خط بنویسید.
3. **آزمایشگاه رباتیک آرَس** — شهریور ۱۴۰۰ تا آبان ۱۴۰۳

یک شغل چهارم هم هست (**تعمیرکار قطعات کامپیوتر، پاسارگاد، ۱۳۹۴–۱۴۰۰**) که به‌صورت **مخفی** گذاشتمش، چون کار مهندسی مکانیک نیست و در یک سایت تخصصی حواس بیننده را پرت می‌کند. اگر دوست دارید کل سابقهٔ کاری‌تان دیده شود، در پنل تیک «Show this job» را بزنید.

شغلی که الان در آن هستید باید `"current": true` داشته باشد — همان است که نقطهٔ نارنجی پُر روی خط زمانی می‌گیرد.

### 🔹 تحصیلات و آموزش

**در پنل:** «Education & certificates» — **در فایل:** `"education"`

- ستون چپ: **تحصیلات** — ارشد مکاترونیک و کارشناسی مکانیک، هر دو خواجه نصیر
- ستون راست: **دوره‌ها و انتشارات** — مقالهٔ ۲۰۲۵ شما، دورهٔ MATLAB و دورهٔ SolidWorks مکتب‌خونه

> اسم این ستون را عمداً «دوره‌ها و انتشارات» گذاشتم، نه «گواهی‌نامه‌ها» — چون در رزومه‌تان نوشته بود این دوره‌ها گواهی ندارند. اگر بعداً گواهی‌نامهٔ واقعی گرفتید، اسم ستون را عوض کنید.

اگر مقاله‌تان لینک آنلاین دارد (مثلاً در ResearchGate یا Google Scholar)، آدرسش را در `link` بگذارید تا یک دکمهٔ «Verify» کنارش بیاید.

### 🔹 تماس

**در پنل:** «Contact» — **در فایل:** `"contact"`

- ایمیل: `saeedmoucavi@gmail.com` (از رزومه)
- تلفن: `0912 205 3808` (از رزومه)
- موقعیت: تهران، ایران
- **لینکدین** ⚠️ آدرس واقعی‌اش را ندارم — در رزومه فقط نوشته بود «Page link». باید خودتان آدرس کاملش را بگذارید.
- واتساپ روی همان شمارهٔ موبایلتان تنظیم شده.

> **یک هشدار دربارهٔ شمارهٔ موبایل:** وقتی شماره‌تان روی یک صفحهٔ عمومی اینترنت باشد، ربات‌ها آن را جمع می‌کنند و احتمال تماس و پیام تبلیغاتی بالا می‌رود. چون در رزومه‌تان هست گذاشتمش، ولی اگر اذیت شدید، در پنل تیک آن ردیف را بردارید و فقط ایمیل و واتساپ بماند.

برای روشن کردن تلگرام یا اینستاگرام: در پنل تیک «Show» آن ردیف را بزنید و آدرستان را بگذارید.

### 🔹 فوتر (پایین صفحه)

**در پنل:** «Footer» — **در فایل:** `"footer"`

سال کپی‌رایت خودش هر سال عوض می‌شود؛ لازم نیست دست بزنید.

---

## ۷. پروژه‌ها: اضافه کردن، حذف کردن، مرتب کردن

الان **۹ پروژهٔ واقعی** از گزارش‌های عملکردتان در سایت هست:

| کد | پروژه | دسته |
|---|---|---|
| PRJ-01 | دستگاه مونتاژ فن و براکت صندلی جلو | اتوماسیون مونتاژ |
| PRJ-02 | فیکسچر پرس دوطرفه مالشی — End forming | فیکسچر و ابزار |
| PRJ-03 | فیکسچر مونتاژی راننده و سرنشین | فیکسچر و ابزار |
| PRJ-04 | فیکسچر کنترلی لینک عقب و جلو | کنترل و بازرسی |
| PRJ-05 | استند رول ایربگ | تجهیزات جابه‌جایی |
| PRJ-06 | فیکسچر تست پشتی صندلی عقب | کنترل و بازرسی |
| PRJ-07 | اصلاح دستگاه برش لوله | بهبود ماشین‌آلات |
| PRJ-08 | ایستگاه لمینه | تجهیزات خط |
| PRJ-09 | برنامهٔ ساخت تجهیزات داخلی خط (۲۵ طرح، ۲۹۰+ دستگاه) | تجهیزات خط |

دکمه‌های فیلتر بالای پروژه‌ها **خودکار** از روی دستهٔ پروژه‌ها ساخته می‌شوند. اگر دستهٔ جدیدی بنویسید، دکمه‌اش خودش اضافه می‌شود.

### اضافه کردن پروژهٔ جدید — در پنل

۱. بروید به `/admin` و پنل **Projects** را باز کنید
۲. تا «My projects» پایین بروید
۳. دکمهٔ **+** را بزنید
۴. جعبه‌ها را پر کنید و **Publish** بزنید

برای **جابه‌جا کردن ترتیب**، پروژه را از دستگیره‌اش بکشید بالا یا پایین.
برای **حذف**، آیکون سطل زباله را بزنید.
برای **مخفی کردن موقت** بدون حذف، تیک «Show this project» را بردارید.

### اضافه کردن پروژهٔ جدید — در فایل

داخل `"projects"` دنبال `"items": [` بگردید. هر پروژه از یک `{` شروع و به `}` ختم می‌شود.
کل یک پروژه را کپی کنید، بعد از آخری پیست کنید، بینشان `,` بگذارید، و این‌ها را عوض کنید:

| فیلد | چه بنویسید |
|---|---|
| `id` | یک کلمهٔ یکتا، مثلاً `"my-new-fixture"` |
| `code` | برچسب کوچک روی کارت، مثلاً `"PRJ-10"` |
| `year` | سال، مثلاً `"1405 / 2026"` |
| `title` | اسم پروژه |
| `category` | دسته — اگر اسم یکی از دسته‌های موجود را بنویسید، در همان گروه می‌رود |
| `client` | سفارش‌دهنده، یا «محرمانه» |
| `summary` | یک جمله — همان که روی کارت دیده می‌شود |
| `problem` | نیازمندی و چالش |
| `solution` | توصیف پروژه — چه ساختید و چطور کار می‌کند |
| `role` | نقش خودتان |
| `outcome` | **سودمندی — مهم‌ترین قسمت. هر جا عدد دارید بنویسید.** |
| `tools` | لیست ابزارها، مثلاً `["SolidWorks", "ANSYS"]` |
| `specs` | جدول مشخصات فنی |
| `cover` | عکس روی کارت |
| `gallery` | عکس‌های داخل پروژه، هرکدام با یک توضیح |

### چطور یک پروژهٔ خوب بنویسیم

از روی تحقیقی که روی نمونه‌های موفق انجام دادم، و از روی کاری که خود شما در گزارش‌هایتان کرده‌اید:

- **سه پروژهٔ کامل بهتر از هفت پروژهٔ نصفه است.** آنهایی که عکس و جزئیات خوب ندارند را حذف کنید.
- **با نتیجه شروع کنید، نه با مشخصات فنی.** «زمان برش به‌شدت کاهش پیدا کرد و لوله‌ها بدون دفورمگی برش می‌خورند» خیلی قوی‌تر از لیست کردن ابعاد فک است. خوشبختانه گزارش‌های خودتان دقیقاً همین‌طور نوشته شده‌اند.
- **ترتیب عکس‌ها را رعایت کنید:** اول رندر سه‌بعدی، بعد عکس مرحلهٔ ساخت، آخر عکس دستگاه نصب‌شده و در حال کار. این ترتیب ثابت می‌کند شما یک طرح را از اول تا آخر می‌برید — و این دقیقاً همان چیزی است که کارفرما دنبالش است.
- **اصطلاح فنی سنگین را در جدول مشخصات بگذارید، نه در توضیح.** ممکن است کسی که تصمیم می‌گیرد شما را استخدام کند مهندس نباشد.
- **اگر پروژه‌ای محرمانه است،** جای اسم کارفرما بنویسید «محرمانه»، مکانیزم را کلی توصیف کنید، و به‌جای عکس سالن، رندر بگذارید.

> ⚠️ **یک نکتهٔ مهم دربارهٔ محرمانگی:** پروژه‌های فعلی از گزارش عملکرد داخلی شما در ماد آریا ویستا برداشته شده و شامل عکس سالن تولید، نام مدل‌های خودرو و شمارهٔ فنی قطعات است. من چون خودتان فایل‌ها را دادید ازشان استفاده کردم، ولی **قبل از اینکه سایت را عمومی کنید، حتماً مطمئن شوید انتشار عمومی این‌ها با قرارداد و سیاست شرکتتان مشکلی ندارد.** اگر تردید دارید، ساده‌ترین کار این است که اسم مدل‌ها را بردارید (مثلاً به‌جای «لینک Envoy» بنویسید «لینک کفی صندلی») و عکس‌های سالن را با رندر جایگزین کنید. من در متن‌ها اسم مدل‌ها را حذف کرده‌ام، ولی روی یکی از عکس‌ها (فیکسچر کنترلی) برچسبی با شمارهٔ فنی دیده می‌شود.

---

## ۸. عکس‌ها و عکس پرسنلی

### در پنل تصویری
روی جعبهٔ عکس کلیک کنید → **Choose an image** → از کامپیوترتان آپلود کنید. تمام.

### در فایل
۱. عکس را بگذارید در `site/assets/images/` (یا `site/assets/images/projects/`)
۲. در `content.json` مسیرش را بنویسید، مثلاً `"photo": "assets/images/saeed.jpg"`

### قانون‌هایی که واقعاً مهم‌اند

| | |
|---|---|
| **عکس پرسنلی** | حدود ۸۰۰ × ۱۰۰۰ پیکسل، عمودی. سایت خودش کمی خاکستری‌اش می‌کند تا با طراحی جور شود. |
| **عکس پروژه** | حدود ۱۶۰۰ × ۱۰۰۰ پیکسل، افقی. |
| **حجم فایل** | هر عکس زیر ۴۰۰ کیلوبایت. با [squoosh.app](https://squoosh.app) عکس را بکشید داخل، کیفیت را روی ۷۵ بگذارید، دانلود کنید. |
| **اسم فایل** | فقط حروف انگلیسی کوچک، عدد و خط تیره: `fixture-render.jpg`. **بدون فاصله، بدون حروف فارسی.** یک فاصله در اسم فایل، عکس را روی سایت زنده خراب می‌کند. |
| **فرمت** | `.jpg` برای عکس، `.png` برای نقشه و رندر با خط تیز. |

### ⚠️ عکس پرسنلی شما هنوز جای خالی است

الان یک طرح خاکستری با نوشتهٔ «YOUR PHOTO GOES HERE» آنجاست. یک عکس پرسنلی خوب — ترجیحاً در محیط کار یا با پس‌زمینهٔ ساده، رسمی ولی نه خشک — بگذارید. این اولین چیزی است که بیننده می‌بیند.

### آیکون‌ها

آیکون‌ها (شکل‌های کوچک روی کارت مهارت‌ها و ردیف‌های تماس) را خود سایت رسم می‌کند — فایلی برای مدیریت وجود ندارد. فقط اسمشان را انتخاب می‌کنید:

- **کارت مهارت‌ها:** `drafting`، `analysis`، `manufacturing`، `automation`، `cog`
- **ردیف‌های تماس:** `email`، `phone`، `location`، `website`
- **دکمه‌های شبکه‌های اجتماعی:** `linkedin`، `whatsapp`، `telegram`، `instagram`، `grabcad`، `website`

اگر آیکونی می‌خواهید که در این لیست نیست، بگویید تا اضافه کنم.

---

## ۹. رنگ‌ها و فونت‌ها

**در پنل:** «Colours & fonts» (هر رنگ یک انتخابگر رنگ درست‌وحسابی دارد) — **در فایل:** `"theme"`

| تنظیم | چه چیزی را کنترل می‌کند |
|---|---|
| `colorBackground` | پس‌زمینهٔ تیرهٔ اصلی |
| `colorSurface` | نوارهای کمی روشن‌تر که یکی‌درمیان می‌آیند |
| `colorSurfaceRaised` | کارت‌ها و جعبه‌ها |
| `colorBorder` | همهٔ خطوط نازک |
| `colorText` | متن اصلی |
| `colorTextMuted` | متن خاکستری فرعی |
| `colorAccent` | **همان نارنجی.** این یکی را عوض کنید، شخصیت کل سایت عوض می‌شود. |
| `colorAccentInk` | رنگ نوشتهٔ روی دکمهٔ نارنجی |
| `colorBlueprint` | خطوط آبی کم‌رنگ نقشه‌ای |
| `showBlueprintGrid` | شبکهٔ کم‌رنگ پس‌زمینه (`true` / `false`) |
| `enableAnimations` | با `false` همهٔ حرکت‌ها و محوشدن‌ها خاموش می‌شود |

رنگ‌ها به شکل `"#F2A33C"` نوشته می‌شوند — یک `#` و شش کاراکتر. رنگ جدید را از [coolors.co](https://coolors.co) بردارید یا از انتخابگر رنگ پنل.

> **یک هشدار دربارهٔ رنگ نارنجی:** اگر آن را به رنگی روشن‌تر عوض کنید، متن مشکی روی دکمه‌ها ممکن است سخت‌خوان شود. بعد از تغییر، رنگ پس‌زمینه و رنگ نارنجی را در [webaim.org/resources/contrastchecker](https://webaim.org/resources/contrastchecker/) بررسی کنید — عدد باید حداقل ۴.۵ باشد.

**فونت‌ها:** `fontDisplay` (تیترها)، `fontBody` (متن)، `fontMono` (برچسب‌های فنی کوچک). فونت فارسی **وزیرمتن** است که خودکار وقتی زبان فارسی است می‌آید. اگر فونت متفاوتی خواستید، باید در `index.html` هم اضافه شود — این تنها تغییری است که به کد نیاز دارد، بگویید تا برایتان انجام دهم.

---

## ۱۰. جابه‌جا کردن و مخفی کردن بخش‌ها

**در پنل:** «Sections — drag to reorder, untick to hide» — **در فایل:** `"sections"`

```json
{ "key": "projects", "enabled": true, "showInNav": true,
  "navLabel": { "en": "Projects", "fa": "پروژه‌ها" } }
```

| تنظیم | کارش |
|---|---|
| `key` | کدام بخش است. **این کلمه را هرگز عوض نکنید.** |
| `enabled` | `true` بخش را نشان می‌دهد، `false` کلاً حذفش می‌کند |
| `showInNav` | `true` یعنی در منوی بالا لینک داشته باشد |
| `navLabel` | متن آن لینک منو |

**ترتیب این لیست، ترتیب صفحه است.** یک بلوک `{ ... }` را بالا یا پایین ببرید، همان بخش در سایت بالا یا پایین می‌رود و لینک منویش هم با خودش می‌رود.

**یک پیشنهاد:** چون پروژه‌های شما قوی‌ترین چیز سایتتان هستند، می‌توانید بلوک `projects` را ببرید بالا، درست بعد از `stats`، تا بازدیدکننده زودتر ببیندشان.

---

## ۱۱. رزومه

رزومهٔ انگلیسی‌تان همین الان روی سایت هست و دکمهٔ **دانلود رزومه** آن را می‌دهد:
`assets/files/cv-saeed-mousavi-en.pdf`

**برای به‌روز کردنش:** فایل PDF جدید را با همین اسم در همین پوشه جایگزین کنید. تمام.

**اگر رزومهٔ فارسی هم دارید:** آن را هم در همان پوشه بگذارید. بگویید تا کاری کنم دکمه بر اساس زبان انتخاب‌شده، رزومهٔ مناسب را بدهد.

---

# بخش سوم — بردن سایت روی اینترنت

## ۱۲. قبل از شروع: دو راه پیش رویتان است

**هزینهٔ کل: فقط دامنه، حدود ۱۰ تا ۱۵ دلار در سال. هاست رایگان است.**
**زمان لازم: حدود یک ساعت و نیم برای بار اول.**

نقشهٔ کلی این است:

```
   فایل‌های شما            گیت‌هاب                  Netlify              دامنهٔ شما
   (پوشهٔ site)   ──────►  (نسخهٔ امن)   ──────►   (منتشرکننده)  ────►  yourname.com
                  یک‌بار              خودکار و همیشگی            اشاره می‌کند
```

### ⚠️ ولی یک مسئلهٔ واقعی هست

Netlify، Vercel و GitHub شرکت‌های آمریکایی هستند. در عمل یعنی:

- **برای ثبت‌نام احتمالاً به VPN نیاز دارید**، و گاهی صفحهٔ ثبت‌نامشان IP ایران را بلاک می‌کند. مواردی از مسدود شدن حساب هم گزارش شده.
- **پرداخت به یک ثبت‌کنندهٔ دامنهٔ خارجی به کارت بین‌المللی نیاز دارد** که از داخل ایران ساده نیست.

پس دو راه دارید. **قبل از اینکه وقت بگذارید، یکی را انتخاب کنید:**

| | راه A — بین‌المللی | راه B — ایرانی |
|---|---|---|
| **مناسب برای** | کارفرما و شریک خارجی | کارخانه‌های داخلی |
| **هاست** | Netlify، رایگان | هاست ایرانی cPanel یا لیارا/ابرآروان |
| **دامنه** | Cloudflare/Porkbun، ~۱۲ دلار | ثبت‌کنندهٔ ایرانی، ریالی |
| **پنل ادمین `/admin`** | ✅ کار می‌کند | ❌ باید فایل را دستی ویرایش کنید |
| **فرم تماس** | ✅ رایگان کار می‌کند | ⚠️ نیاز به تنظیم متفاوت |
| **پرداخت** | کارت بین‌المللی لازم است | ریالی، بدون دردسر |

**راه C هم هست:** هر دو. یک `.com` روی Netlify برای مخاطب خارجی، و همان فایل‌ها روی یک هاست ایرانی با `.ir` برای مخاطب داخلی. دقیقاً همان پوشه، دو بار آپلود.

قدم‌های ۱ تا ۸ که در ادامه می‌آید **راه A** را توضیح می‌دهد. اگر راه B را انتخاب کردید، قدم ۱ (دامنه) را بخوانید و بعد بروید سراغ [بخش ۲۱](#۲۱-راه-ایران--اگر-netlify-جواب-نداد).

---

## قدم ۱ — خریدن دامنه

### چه اسمی بگیرم؟

**بهترین انتخاب:** `saeedmousavi.com` — اسم خودتان. تا آخر عمر حرفه‌ای‌تان درست می‌ماند، همان چیزی است که آدم‌ها سرچ می‌کنند، و همان چیزی است که کارفرما انتظار دارد روی رزومه‌تان ببیند.

اگر گرفته شده بود، به ترتیب اولویت:
1. با حرف وسط: `saeedmmousavi.com`
2. `mousavi.engineer` یا `saeedmousavi.engineer` — پسوند `.engineer` واقعی است و به کار شما خیلی می‌آید
3. `saeedmousavi.me` یا `.co`
4. توصیفی: `mousavi-machinery.com` — اگر می‌خواهید بیشتر شبیه یک شرکت به نظر برسد

### چه چیزهایی را رعایت کنم

| بکنید | نکنید |
|---|---|
| کوتاه باشد و پای تلفن راحت گفته شود | تا می‌توانید از خط تیره استفاده نکنید — «دَش» پای تلفن دردسر است |
| `.com` را ترجیح بدهید | عدد نگذارید («۴ بود یا four؟») |
| **قیمت سال دوم** را چک کنید | گول ۱ دلار سال اول را نخورید که سال بعد ۴۰ دلار می‌شود |
| حریم خصوصی WHOIS رایگان بگیرید | برای حریم خصوصی پول ندهید — جاهای خوب رایگان می‌دهند |
| اول یک سال بخرید | ۱۰ سال یکجا نخرید تا مطمئن شوید |

### از کجا بخرم

| ثبت‌کننده | تقریباً | توضیح |
|---|---|---|
| **Cloudflare Registrar** ⭐ | ۱۰ دلار/سال | **پیشنهاد من.** به قیمت تمام‌شده می‌فروشد، سود اضافه نمی‌گیرد، حریم خصوصی رایگان، و قیمت تمدید = قیمت سال اول. |
| **Porkbun** | ۱۰–۱۱ دلار | خیلی خوب، محیط ساده، برای بار اول راحت‌ترین است |
| **Namecheap** | ۱۰–۱۵ دلار | معتبر و پرطرفدار. حواستان به قیمت تمدید باشد |
| **خود Netlify** | ۱۵ دلار | کمی گران‌تر، ولی قدم ۵ تبدیل می‌شود به یک کلیک |
| ~~GoDaddy~~ | — | نگیرید. سال اول ارزان، تمدید گران، و مدام چیز اضافه می‌فروشد |

### چطور

۱. در سایت ثبت‌کننده اسم مورد نظرتان را جستجو کنید
۲. اگر آزاد بود، به سبد اضافه کنید. **همهٔ چیزهای اضافه‌ای که پیشنهاد می‌دهند را رد کنید** — به هاست، ایمیل، SSL و «محافظت از سایت» آنها نیازی ندارید
۳. پرداخت کنید
۴. **تمدید خودکار را روشن کنید.** آدم‌ها دامنه‌شان را از یاد بردن تمدید از دست می‌دهند، و برگرداندنش یا خیلی گران است یا ناممکن

---

## قدم ۲ — انتخاب هاست

### جواب کوتاه: Netlify. رایگان است و دقیقاً ابزار درست برای این سایت.

سایت شما فقط HTML و CSS و JavaScript ساده است. نه دیتابیس می‌خواهد، نه PHP. یعنی بهترین هاست برایش یک **هاست استاتیک** است — و هاست استاتیک در این حجم ترافیک رایگان است.

| گزینه | هزینه | نظر |
|---|---|---|
| **Netlify** ⭐ | **رایگان** | **پیشنهاد من.** هاست رایگان، SSL رایگان، CDN جهانی رایگان، به‌علاوهٔ دو چیزی که این سایت واقعاً استفاده می‌کند: فرم تماس (ماهی ۱۰۰ پیام رایگان) و سیستم ورود پشت پنل `/admin`. هیچ جای دیگری این سه‌تا را با هم رایگان نمی‌دهد. |
| Vercel | رایگان | برای هاست به همان خوبی، ولی فرم و سیستم ورود ندارد؛ پس `/admin` و فرم تماس هر دو تنظیم اضافه می‌خواهند |
| Cloudflare Pages | رایگان | عالی و خیلی سریع. همان دو کمبود Vercel |
| GitHub Pages | رایگان | برای هاست خوب است، ولی نه فرم دارد نه ورود |
| هاست اشتراکی cPanel | ۳۰–۷۰ دلار/سال | کار می‌کند، ولی پول دیتابیس و PHP‌ای را می‌دهید که هیچ‌وقت استفاده نمی‌کنید |

**رایگان یعنی واقعاً رایگان، نه دورهٔ آزمایشی.** پلن رایگان Netlify یک محصول دائمی است — ماهی ۱۰۰ گیگابایت ترافیک، که یک سایت رزومه‌ای هیچ‌وقت به آن نمی‌رسد.

---

## قدم ۳ — گذاشتن فایل‌ها روی گیت‌هاب

گیت‌هاب سایت شما را نگه می‌دارد و تاریخچهٔ همهٔ تغییرات را ذخیره می‌کند، پس هیچ‌وقت چیزی از دست نمی‌رود.

۱. بروید به [github.com](https://github.com) و یک حساب رایگان بسازید
۲. روی **+** گوشهٔ بالا راست بزنید → **New repository**
۳. اسمش را بگذارید `portfolio`. اگر خواستید **Private** بگذارید. **هیچ‌کدام** از تیک‌های «add a README» را نزنید. → **Create repository**
۴. در صفحهٔ بعد روی **uploading an existing file** کلیک کنید
۵. پوشهٔ `site` را روی کامپیوترتان باز کنید. **همهٔ چیزهای داخلش** را انتخاب کنید (`index.html`، `content.json`، پوشهٔ `admin`، پوشهٔ `assets` و بقیه) و بکشید داخل پنجرهٔ مرورگر

   > ⚠️ **خیلی مهم:** **محتویات** پوشهٔ `site` را آپلود کنید، نه خود پوشه را.
   > بعد از آپلود، گیت‌هاب باید `index.html` را در بالاترین سطح نشان بدهد. اگر یک پوشهٔ تنها به اسم `site` می‌بینید، اشتباه شده — پاکش کنید و دوباره آپلود کنید.

۶. صبر کنید آپلود تمام شود، بعد دکمهٔ سبز **Commit changes** را بزنید

---

## قدم ۴ — انتشار با Netlify

۱. بروید به [netlify.com](https://netlify.com) → **Sign up** → **Sign up with GitHub** را انتخاب کنید و اجازه بدهید
۲. در داشبورد: **Add new site** → **Import an existing project** → **GitHub**
۳. از لیست، مخزن `portfolio` را انتخاب کنید
۴. Netlify تنظیمات ساخت را نشان می‌دهد. چون چیزی برای ساختن نیست:
   - **Build command:** کاملاً خالی بگذارید
   - **Publish directory:** خالی بگذارید (یا `.` بنویسید)

   فایل `netlify.toml` این‌ها را از قبل درست تنظیم کرده، پس معمولاً پیش‌فرض‌ها درست‌اند.
۵. **Deploy** را بزنید

حدود سی ثانیه بعد سایتتان روی یک آدرس تصادفی مثل `https://sparkling-otter-4a2b91.netlify.app` زنده است. **بازش کنید و ببینید کار می‌کند.**

> برای اینکه آن آدرس موقت قشنگ‌تر شود: **Site configuration → Change site name** → بنویسید `saeedmousavi`. می‌شود `saeedmousavi.netlify.app`.

**از این لحظه به بعد، سایتتان خودش را به‌روز می‌کند.** هر تغییری که در گیت‌هاب ذخیره شود — از جمله هر بار که در پنل `/admin` دکمهٔ Publish را بزنید — تا یک دقیقه بعد روی سایت زنده می‌آید.

---

## قدم ۵ — وصل کردن دامنه

در Netlify: **Domain management** → **Add a domain** → دامنه‌تان را بنویسید → **Verify** → **Add**

بعد Netlify می‌گوید چه کار کنید. دو راه هست:

### راه A — عوض کردن نیم‌سرورها (ساده‌تر، پیشنهاد من)

Netlify چهار آدرس مثل `dns1.p03.nsone.net` به شما نشان می‌دهد.

۱. وارد پنل **ثبت‌کنندهٔ دامنه** شوید (جایی که دامنه را خریدید)
۲. دنبال تنظیمی به اسم **Nameservers** بگردید (گاهی «DNS» یا «Custom DNS»)
۳. آنهایی که هست را پاک کنید و چهار آدرس Netlify را بگذارید. ذخیره کنید

### راه B — نگه داشتن DNS ثبت‌کننده

اگر ترجیح می‌دهید نیم‌سرورها دست نخورند، این دو رکورد را در پنل DNS ثبت‌کننده اضافه کنید:

| نوع | نام | مقدار |
|---|---|---|
| `A` | `@` | `75.2.60.5` |
| `CNAME` | `www` | `اسم-سایت-شما.netlify.app` |

> Netlify مقدارهای دقیق مربوط به سایت خودتان را روی صفحه نشان می‌دهد — **آنها را بگذارید**، نه اینها را، اگر فرق داشتند.

### بعدش صبر کنید

تغییرات DNS بین **۱۰ دقیقه تا ۴۸ ساعت** (معمولاً حدود یک ساعت) در کل اینترنت پخش می‌شود. در این مدت ممکن است سایت برای شما باز شود ولی برای دوستتان نه، یا برعکس. **این طبیعی است. در این مدت هیچ تنظیمی را عوض نکنید** — عوض کردن، ساعت را از اول شروع می‌کند.

پیشرفت را در [dnschecker.org](https://dnschecker.org) ببینید — دامنه را بنویسید و تیک‌های سبز را دور دنیا تماشا کنید.

---

## قدم ۶ — روشن کردن قفل سبز (HTTPS)

HTTPS همان قفل کوچک کنار آدرس در مرورگر است. بدون آن، مرورگر به بازدیدکننده هشدار **«Not secure»** نشان می‌دهد — که برای سایتی که قرار است روی کارفرمای صنعتی تأثیر بگذارد فاجعه است.

**در Netlify خودکار است.** بعد از اینکه دامنه وصل شد (قدم ۵)، بروید به **Domain management → HTTPS**. باید ببینید گواهی Let's Encrypt صادر شده. اگر نوشته «waiting on DNS propagation»، یک ساعت صبر کنید و **Verify DNS configuration** را بزنید.

بعد **Force HTTPS** را روشن کنید تا هر کسی `http://` تایپ کرد، خودکار به نسخهٔ امن برود.

**تست کنید:** `https://دامنه‌تان.com` را باز کنید و قفل را ببینید. بعد `http://دامنه‌تان.com` را امتحان کنید و مطمئن شوید خودش می‌پرد روی نسخهٔ قفل‌دار.

---

## قدم ۷ — روشن کردن پنل ویرایش

این همان چیزی است که به شما اجازه می‌دهد سایت را از هر کامپیوتر یا موبایلی، بدون کد، ویرایش کنید. حدود پنج دقیقه، فقط یک بار.

۱. در Netlify: **Site configuration** → **Identity** → **Enable Identity**
۲. هنوز در Identity → **Registration preferences** → روی **Invite only** بگذارید
   > ⚠️ **این را رد نکنید.** اگر روی «Open» بماند، هر غریبه‌ای می‌تواند ثبت‌نام کند و سایت شما را ویرایش کند.
۳. پایین‌تر بروید تا **Services → Git Gateway** → **Enable Git Gateway**
   > این همان قدمی است که اکثر آدم‌ها فراموش می‌کنند و بعد نمی‌توانند وارد شوند.
۴. بروید به تب **Identity** بالای صفحه → **Invite users** → ایمیل خودتان را بزنید → **Send**
۵. ایمیلتان را چک کنید، روی لینک دعوت کلیک کنید، یک رمز انتخاب کنید
۶. بروید به `https://دامنه‌تان.com/admin`، وارد شوید، و کل سایتتان را به شکل جعبه‌های قابل ویرایش می‌بینید

**روش کار از این به بعد:** `/admin` را باز کنید، یک پنل را باز کنید، تغییر بدهید، **Publish** بزنید. یک دقیقه صبر کنید. سایت را رفرش کنید. همین.

> اگر مخزنتان **Private** است، در گیت‌هاب هم بروید به **Settings → Applications** و مطمئن شوید Netlify به آن دسترسی دارد.

---

## قدم ۸ — پیام‌های فرم تماس

چیزی برای تنظیم کردن نیست — فرم به‌محض اینکه سایت روی Netlify باشد کار می‌کند.

۱. از روی سایت زنده‌تان یک پیام آزمایشی برای خودتان بفرستید
۲. در Netlify بروید به **Forms**. پیام آزمایشی‌تان باید آنجا باشد
۳. روی فرم `contact` کلیک کنید → **Settings & usage** → **Form notifications** → **Add notification → Email notification** → ایمیلتان را بزنید

حالا هر درخواستی هم در Netlify ذخیره می‌شود، هم به ایمیلتان می‌آید.

> **اسپم:** فرم از قبل یک تلهٔ مخفی دارد که بیشتر ربات‌ها را می‌گیرد. اگر باز هم اسپم آمد، در همان صفحهٔ تنظیمات reCAPTCHA خود Netlify را روشن کنید.

---

## ۲۱. راه ایران — اگر Netlify جواب نداد

خبر خوب: **سایت شما فایل‌های ساده است، پس هر هاستی روی زمین می‌تواند آن را سرو کند.**

### دامنه
از یک ثبت‌کنندهٔ ایرانی بگیرید (مثلاً پارس‌دیتا، ایران‌سرور، هاست‌ایران، پارس‌پک). دامنهٔ `.ir` از طریق ایرنیک ثبت می‌شود و ارزان است؛ `.com` را هم همین شرکت‌ها ریالی می‌فروشند.

### هاست
هر پلن هاست اشتراکی معمولی با cPanel کافی است — حدود ۵۰۰ هزار تا ۲ میلیون ریال در سال. یا پلتفرم‌های ایرانی مثل **لیارا** یا **ابرآروان** که خیلی شبیه Netlify کار می‌کنند.

### چطور منتشر کنم
۱. وارد cPanel هاستتان شوید
۲. **File Manager** را باز کنید
۳. بروید داخل پوشهٔ `public_html`
۴. **همهٔ محتویات پوشهٔ `site`** را آپلود کنید (اگر ZIP کردید، آنجا Extract کنید)
۵. تمام. سایت بالاست

برای به‌روزرسانی، فایل‌های تغییرکرده را دوباره آپلود می‌کنید (معمولاً فقط `content.json` و عکس‌های جدید).

### SSL
هر هاست ایرانی SSL رایگان Let's Encrypt دارد. در cPanel دنبال **SSL/TLS Status** یا **Let's Encrypt SSL** بگردید، دامنه‌تان را تیک بزنید، **Run AutoSSL** یا **Issue** را بزنید. بعد **Force HTTPS Redirect** را هم روشن کنید.

> اگر هاستی بابت SSL پول خواست، هاست عوض کنید. هیچ‌کس نباید بابت این پول بگیرد.

### دو چیزی که فرق می‌کند

**۱. پنل `/admin` کار نمی‌کند** — چون به سیستم ورود Netlify وابسته است. به‌جایش `content.json` را دستی ویرایش می‌کنید (بخش ۴، راه دوم) و دوباره آپلودش می‌کنید. با راهنمای بخش دوم این کار کاملاً شدنی است.

**۲. فرم تماس تنظیم متفاوتی می‌خواهد.** سه گزینه دارید، در `content.json` قسمت `contact.formProvider`:

| مقدار | چه اتفاقی می‌افتد | تنظیم لازم |
|---|---|---|
| `"netlify"` | فقط روی Netlify کار می‌کند | — |
| `"formspree"` | پیام‌ها با سرویس Formspree ایمیل می‌شوند، ماهی ۵۰ تا رایگان | در formspree.io ثبت‌نام کنید و آدرس فرمتان را در `formspreeEndpoint` بگذارید |
| `"mailto"` | برنامهٔ ایمیل خود بازدیدکننده با متن آماده باز می‌شود | — ولی خیلی‌ها برنامهٔ ایمیل تنظیم‌شده ندارند و بی‌سروصدا شکست می‌خورد |

یا کلاً فرم را بردارید (`"showForm": false`) و فقط ایمیل و واتساپ بماند — که برای مخاطب ایرانی صادقانه بگویم احتمالاً **مؤثرتر** هم هست، چون بیشتر کارفرماها ترجیح می‌دهند مستقیم واتساپ بزنند.

**بگویید کدام راه را انتخاب کردید تا فرم و پوشهٔ ادمین را متناسبش تنظیم کنم.**

---

# بخش چهارم — بعد از انتشار

## ۲۲. دیده شدن در گوگل (SEO)

### اول این سه جا را درست کنید

دنبال `YOUR-DOMAIN-HERE.com` بگردید و آدرس واقعی‌تان را جایش بگذارید در:
- `robots.txt`
- `sitemap.xml`
- `content.json` → `site` → `siteUrl`

### به گوگل بگویید سایتتان هست

۱. بروید به [search.google.com/search-console](https://search.google.com/search-console)
۲. **Add property** → **URL prefix** → آدرس کامل `https://` سایتتان
۳. مالکیت را تأیید کنید — روش **HTML tag** را انتخاب کنید، کدی که می‌دهد را کپی کنید، و برای گذاشتنش در `index.html` از من کمک بخواهید (فقط یک خط است)
۴. بعد از تأیید: **Sitemaps** → بنویسید `sitemap.xml` → **Submit**

گوگل معمولاً یک سایت جدید را بین چند روز تا دو هفته ثبت می‌کند.

### چیزهایی که واقعاً برای شما فرق می‌کنند

- **عنوان و توضیح صفحه** در `content.json` → `site` → `pageTitle` و `metaDescription` است. طوری بنویسید که کسی که دنبال کار شما می‌گردد پیدایتان کند.
- **از کلماتی استفاده کنید که کارفرمای شما سرچ می‌کند.** اگر کارخانه‌ها دنبال «طراحی فیکسچر»، «ساخت دستگاه مونتاژ» یا «تجهیزات خط تولید» می‌گردند، همین عبارت‌ها باید در متن درباره من و پروژه‌هایتان باشد. زورکی تکرارشان نکنید — فقط طبیعی بنویسید.
- **متن پروژه‌هایتان همان SEO شماست.** تنها متن مفصل و تخصصی سایت شماست. خوشبختانه گزارش‌های شما پر از اصطلاح دقیق صنعتی است — همین باعث می‌شود در جستجوهای تخصصی پیدا شوید.
- **اسم فایل عکس‌ها را معنادار بگذارید** — `fixture-end-forming.jpg` نه `IMG_2847.jpg`. گوگل اسم فایل را می‌خواند. (عکس‌های فعلی را از قبل معنادار اسم‌گذاری کرده‌ام.)
- سایت از قبل sitemap، robots و دادهٔ ساختاریافته دارد که به گوگل می‌گوید شما یک شخص با عنوان شغلی، ایمیل و پروفایل شبکه‌های اجتماعی هستید.

---

## ۲۳. تست نهایی قبل از اینکه به کسی نشان بدهید

- [ ] سایت را روی موبایل خودتان باز کنید. کاملش را بخوانید. همهٔ دکمه‌ها را بزنید
- [ ] زبان را انگلیسی کنید و دوباره همه را بخوانید — چیدمان باید درست آینه شود
- [ ] هر پروژه را باز کنید و کل گالری عکسش را ورق بزنید
- [ ] از فرم تماس یک پیام بفرستید و ببینید می‌رسد
- [ ] روی دکمهٔ لینکدین و واتساپ بزنید و ببینید جای درستی می‌روند
- [ ] رزومهٔ خودتان را از روی دکمه دانلود کنید
- [ ] آدرس را در [pagespeed.web.dev](https://pagespeed.web.dev) بزنید — باید نمرهٔ خیلی بالایی بگیرد
- [ ] آدرس را در واتساپ برای خودتان بفرستید و ببینید کارت پیش‌نمایشش درست است
- [ ] از دو نفر بخواهید نگاه کنند — یکی مهندس، یکی غیرمهندس — و بگویند فکر می‌کنند شما چه کاره‌اید. اگر غیرمهندس نتوانست بگوید، متن معرفی را ساده‌تر کنید

### آمار بازدید (اختیاری)

| ابزار | هزینه |
|---|---|
| Netlify Analytics | ۹ دلار در ماه، یک کلیک، بدون کد |
| Google Analytics | رایگان، ولی کد می‌خواهد |
| Plausible / Umami | ساده و خصوصی |

**نظر صادقانهٔ من: فعلاً بی‌خیال آمار شوید.** برای یک سایت شخصی، عددی که اهمیت دارد این است که چند تا درخواست به ایمیلتان می‌رسد — و آن را از فرم تماس دارید. بعداً اگر واقعاً خواستید اضافه کنید.

### و بعد

- آدرس را بگذارید روی رزومه، پروفایل لینکدین، امضای ایمیل و کارت ویزیتتان
- در پروفایل لینکدین، فیلد **Website** را پر کنید
- یک یادآور بگذارید برای **یازده ماه بعد** تا چک کنید دامنه تمدید شده

---

## ۲۴. وقتی چیزی خراب شد

**سایت می‌نویسد «The site content could not be loaded»**
یک تایپوی کوچک در `content.json` دارید — تقریباً همیشه یک کاما یا گیومهٔ پاک‌شده. فایل را در [jsonlint.com](https://jsonlint.com) پیست کنید تا خط دقیقش را بگوید.

**یک عکس نمی‌آید / آیکون شکسته نشان می‌دهد**
- اسم فایل در `content.json` باید **دقیقاً** با فایل واقعی یکی باشد، حتی حروف بزرگ و کوچک. روی سرور `Photo.JPG` و `photo.jpg` دو فایل متفاوت‌اند
- اسم فایل نباید فاصله داشته باشد
- مطمئن شوید فایل واقعاً در همان پوشه‌ای است که مسیر می‌گوید

**تغییری که دادم نمی‌آید**
- در پیش‌نمایش: `Ctrl + F5` بزنید
- در سایت زنده: Netlify حدود یک دقیقه وقت می‌خواهد. صبر کنید بعد `Ctrl + F5`

**نمی‌توانم وارد `/admin` شوم**
هر سه را چک کنید: Identity روشن است، **Git Gateway روشن است** (این همانی است که همه فراموش می‌کنند)، و دعوت ایمیلی را قبول کرده‌اید. اگر لینک دعوت منقضی شده، یکی جدید بفرستید.

**دامنه هنوز صفحهٔ پارک ثبت‌کننده را نشان می‌دهد**
DNS هنوز پخش نشده. تا ۴۸ ساعت صبر کنید. در [dnschecker.org](https://dnschecker.org) چک کنید. در این مدت تنظیمات را عوض نکنید.

**مرورگر هشدار «Not secure» می‌دهد**
گواهی هنوز صادر نشده. Netlify → **Domain management → HTTPS** → **Verify DNS configuration**.

**همه‌چیز را خراب کردم و می‌خواهم از اول شروع کنم**
هر بار که در پنل ادمین Publish می‌زنید، نسخهٔ قبلی در تاریخچهٔ گیت‌هاب می‌ماند. یعنی هیچ‌وقت واقعاً چیزی از دست نمی‌رود. بگویید تا نسخهٔ قبلی را برگردانم — یک دقیقه کار دارد.

---

## ۲۵. ⚠️ کارهایی که حتماً باید خودتان انجام دهید

سایت با اطلاعات واقعی شما پر شده، ولی این چند مورد از دست من برنمی‌آمد. **قبل از اینکه سایت را به کسی نشان دهید، این‌ها را انجام دهید:**

### حتماً

- [ ] **عکس پرسنلی‌تان را بگذارید** — الان جای خالی است
- [ ] **آدرس کامل پروفایل لینکدین‌تان** — در رزومه فقط «Page link» نوشته بود
- [ ] **شرح شغل تلاش B.S.R** — این شغل در سایت هست ولی توضیحش خالی است
- [ ] **چهار عدد بالای صفحه را تأیید کنید** (۵+ سال، ۳۰+ پروژه، ۲۹۰+ دستگاه، ۶ سالن) — من از روی گزارش‌هایتان حساب کردم
- [ ] **درصد مهارت‌ها را مرور کنید** — از روی رزومه‌تان گذاشتم، ولی این ادعای شماست
- [ ] **سه پاراگراف «درباره من» را بخوانید** و اگر جایی حرفم با حرف شما فرق دارد اصلاح کنید
- [ ] **دربارهٔ محرمانگی تصمیم بگیرید** — پروژه‌ها از گزارش داخلی شرکتتان است. مطمئن شوید انتشار عمومی‌شان مشکلی ندارد (بخش ۷ را ببینید)
- [ ] بعد از خرید دامنه، `YOUR-DOMAIN-HERE.com` را در `robots.txt`، `sitemap.xml` و `content.json` عوض کنید

### خوب است

- [ ] برای پروژه‌ها **عدد** پیدا کنید — «زمان سیکل از X به Y رسید»، «ضایعات Z درصد کم شد». گزارش‌هایتان کیفی نوشته شده؛ حتی یک عدد واقعی، قدرت هر پروژه را چند برابر می‌کند
- [ ] اگر عکس بیشتری از پروژه‌ها دارید (مرحلهٔ ساخت، نصب روی خط، دستگاه در حال کار) اضافه کنید — ترتیب رندر ← ساخت ← نصب خیلی قوی است
- [ ] اگر رزومهٔ فارسی هم دارید بفرستید تا اضافه کنم
- [ ] اگر لینک آنلاین مقاله‌تان را دارید اضافه کنید

---

## سؤالی داشتید؟

هر جای این راهنما گیر کردید یا چیزی خواستید که اینجا نیست، فقط بپرسید. مخصوصاً برای این‌ها حتماً بگویید:

- می‌خواهید نوارهای درصد مهارت برداشته شوند
- راه ایرانی را انتخاب کردید (تا فرم و ادمین را تنظیم کنم)
- می‌خواهید دکمهٔ رزومه بر اساس زبان، فایل فارسی یا انگلیسی بدهد
- می‌خواهید آیکون یا فونت جدیدی اضافه شود
- می‌خواهید ترتیب بخش‌ها عوض شود

موفق باشید 🔧
