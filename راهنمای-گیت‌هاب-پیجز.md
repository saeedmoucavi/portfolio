# راهنمای انتشار روی GitHub Pages

سلام سعید 👋

این راهنما سایت شما را روی **GitHub Pages** بالا می‌آورد — رایگان، بدون کارت بین‌المللی، و با پنل ویرایش تصویری `/admin` که کامل کار می‌کند.

> **قبل از شروع بدانید:** برای اینکه GitHub Pages رایگان باشد، مخزن (repository) باید **Public** باشد. یعنی فایل‌های سایت (از جمله عکس‌های پروژه‌ها) روی github.com هم برای عموم قابل دیدن می‌شوند، نه فقط سایت نهایی. چون این عکس‌ها از گزارش داخلی شرکت شماست، **اول مطمئن شوید انتشار عمومی‌شان اشکالی ندارد** (بخش محرمانگی راهنمای اصلی را ببینید).

کل کار سه قسمت است:
- **الف)** سایت را روی گیت‌هاب بگذارید و منتشرش کنید (۱۵ دقیقه) — بعد از این، سایتتان زنده است
- **ب)** پنل ویرایش `/admin` را راه بیندازید (۲۰ دقیقه، یک بار) — بعد از این، بدون کد ویرایش می‌کنید
- **ج)** دامنهٔ دلخواه وصل کنید (اختیاری)

اگر فقط قسمت الف را انجام دهید، سایت زنده است و محتوا را می‌توانید مستقیم روی خود گیت‌هاب ویرایش کنید. قسمت ب فقط برای این است که پنل قشنگ `/admin` را داشته باشید.

---

## قسمت الف — بردن سایت روی اینترنت

### قدم ۱ — ساختن حساب و مخزن گیت‌هاب

۱. بروید به [github.com](https://github.com) و یک حساب رایگان بسازید (اگر ندارید).
   > ثبت‌نام گیت‌هاب گاهی از داخل ایران VPN می‌خواهد. برخلاف Netlify، حساب را مسدود نمی‌کند.
۲. روی **+** گوشهٔ بالا-چپ بزنید → **New repository**.
۳. تنظیمات:
   - **Repository name:** `portfolio` (یا هر اسمی که دوست دارید)
   - **Public** را انتخاب کنید (برای Pages رایگان لازم است)
   - هیچ‌کدام از تیک‌های «Add a README / .gitignore / license» را نزنید
۴. **Create repository** را بزنید.

اسم کاربری‌تان و اسم مخزن را یادداشت کنید — مثلاً `saeedmousavi/portfolio`. بعداً لازمشان دارید.

### قدم ۲ — آپلود فایل‌های سایت

۱. در صفحهٔ مخزن، روی **uploading an existing file** کلیک کنید.
۲. پوشهٔ `site` را روی کامپیوترتان باز کنید.
۳. **همهٔ چیزهای داخل `site`** را انتخاب کنید و بکشید داخل مرورگر:
   `index.html`، `content.json`، `favicon.svg`، `.nojekyll`، پوشهٔ `admin`، پوشهٔ `assets` و بقیه.

   > ⚠️ **مهم:** محتویات *داخل* `site` را آپلود کنید، نه خود پوشهٔ `site` را.
   > بعد از آپلود، گیت‌هاب باید `index.html` را در بالاترین سطح نشان بدهد.
   >
   > ⚠️ **فایل `.nojekyll` را حتماً آپلود کنید** — چون اسمش با نقطه شروع می‌شود گاهی در ویندوز پنهان است. اگر آن را نبینید، در File Explorer از منوی View گزینهٔ «Hidden items» را روشن کنید. این فایل جلوی یک مشکل رایج GitHub Pages را می‌گیرد.

۴. پایین صفحه دکمهٔ سبز **Commit changes** را بزنید.

### قدم ۳ — روشن کردن GitHub Pages

۱. در مخزن، بالای صفحه روی **Settings** بزنید.
۲. از منوی چپ، **Pages** را انتخاب کنید.
۳. زیر **Build and deployment → Source**، گزینهٔ **Deploy from a branch** را انتخاب کنید.
۴. زیر **Branch**: شاخهٔ **main** و پوشهٔ **/ (root)** را انتخاب کنید → **Save**.
۵. حدود یک تا دو دقیقه صبر کنید، صفحه را رفرش کنید. بالای همان صفحه آدرس سایتتان ظاهر می‌شود:

   ```
   https://اسم-کاربری.github.io/portfolio/
   ```

**سایتتان زنده است! 🎉** بازش کنید و ببینید کار می‌کند.

> **HTTPS (قفل سبز):** GitHub Pages خودکار قفل سبز را روشن می‌کند — کاری لازم نیست. اگر می‌خواهید مطمئن شوید، در همان صفحهٔ Pages تیک **Enforce HTTPS** را بزنید.

---

## قسمت ب — راه‌اندازی پنل ویرایش `/admin`

این قسمت یک بار انجام می‌شود و بعدش برای همیشه بدون کد ویرایش می‌کنید. سه کار دارد:
۱. یک «برنامهٔ ورود» (OAuth App) در گیت‌هاب بسازید
۲. یک «واسط ورود» رایگان روی Cloudflare بگذارید
۳. دو خط در فایل تنظیمات پنل پر کنید

> **چرا این کار لازم است؟** پنل `/admin` برای ذخیره‌کردن تغییرات باید با اجازهٔ شما وارد گیت‌هاب شود. گیت‌هاب این اجازه را مستقیم به یک صفحهٔ ساده نمی‌دهد؛ یک واسط کوچک لازم است که این وسط بنشیند. آن واسط را رایگان روی Cloudflare می‌گذاریم.

### قدم ۴ — ساختن برنامهٔ ورود در گیت‌هاب

۱. بروید به [github.com/settings/developers](https://github.com/settings/developers).
۲. **OAuth Apps** → **New OAuth App**.
۳. پر کنید:
   - **Application name:** `Portfolio Admin` (هر چیزی)
   - **Homepage URL:** آدرس سایتتان، مثلاً `https://saeedmousavi.github.io/portfolio/`
   - **Authorization callback URL:** فعلاً موقتاً بگذارید `https://example.com/callback` — در قدم بعد آدرس واقعی را می‌آورید و برمی‌گردید اینجا اصلاحش می‌کنید.
۴. **Register application** را بزنید.
۵. حالا دو چیز می‌بینید که لازمشان دارید:
   - **Client ID** — همین‌جا نمایش داده می‌شود، کپی کنید.
   - **Client secret** — روی **Generate a new client secret** بزنید، بعد کپی کنید.
     > 🔒 این «راز» را جایی امن نگه دارید و به کسی ندهید. فقط یک بار کامل نمایش داده می‌شود.

این صفحه را باز نگه دارید؛ قدم بعد برمی‌گردید.

### قدم ۵ — گذاشتن واسط ورود روی Cloudflare

۱. بروید به [dash.cloudflare.com](https://dash.cloudflare.com) و یک حساب رایگان بسازید.
۲. از منوی چپ: **Workers & Pages** → **Create** → **Create Worker**.
۳. یک اسم بگذارید، مثلاً `portfolio-login`. → **Deploy** (فعلاً کد پیش‌فرض را deploy می‌کند).
۴. بعد از deploy، روی **Edit code** بزنید.
۵. کل کد داخل ویرایشگر را پاک کنید، و به‌جایش **کل محتوای فایل `cloudflare-oauth-worker.js`** را (که کنار پوشهٔ `site` است) کپی و پیست کنید.
۶. بالا-راست **Deploy** را بزنید.
۷. حالا دو راز گیت‌هاب را به Worker بدهید:
   - از منوی Worker بروید به **Settings** → **Variables and Secrets** (یا **Variables**).
   - دو متغیر **Add** کنید (نوعشان را **Secret** بگذارید اگر پرسید):
     - نام: `GITHUB_CLIENT_ID` — مقدار: همان Client ID از قدم ۴
     - نام: `GITHUB_CLIENT_SECRET` — مقدار: همان Client secret از قدم ۴
   - **Save / Deploy** بزنید.
۸. آدرس Worker شما بالای صفحه‌اش نوشته شده، چیزی مثل:
   ```
   https://portfolio-login.اسم-شما.workers.dev
   ```
   کپی‌اش کنید.

**حالا برگردید به صفحهٔ OAuth App گیت‌هاب (قدم ۴)** و **Authorization callback URL** را اصلاح کنید به:
```
https://portfolio-login.اسم-شما.workers.dev/callback
```
(همان آدرس Worker + `/callback` در انتهایش) → **Update application**.

### قدم ۶ — پر کردن دو خط در تنظیمات پنل

فایل `site/admin/config.yml` را باز کنید (با Notepad). در بالای فایل این دو خط را پیدا کنید و پرشان کنید:

```yaml
backend:
  name: github
  repo: YOUR-GITHUB-USERNAME/YOUR-REPOSITORY-NAME   # ← اینجا
  branch: main
  base_url: https://YOUR-LOGIN-HELPER.workers.dev    # ← و اینجا
```

- `repo:` را بگذارید اسم کاربری و مخزنتان، مثلاً `saeedmousavi/portfolio`
- `base_url:` را بگذارید آدرس Worker (بدون `/callback`)، مثلاً `https://portfolio-login.اسم-شما.workers.dev`

ذخیره کنید، و این فایل تغییرکرده را روی گیت‌هاب آپلود کنید (مثل قدم ۲، همان فایل را دوباره بالا بگذارید و Commit بزنید). یک دقیقه صبر کنید.

### قدم ۷ — امتحان کنید

بروید به:
```
https://اسم-کاربری.github.io/portfolio/admin/
```

باید دکمهٔ **Login with GitHub** را ببینید. بزنیدش، به گیت‌هاب اجازه بدهید، و وارد پنل می‌شوید. حالا هر تغییری بدهید و **Publish** بزنید، تا یک دقیقه بعد روی سایت زنده می‌آید.

**تمام شد.** از این به بعد فقط `/admin` را باز می‌کنید و ویرایش می‌کنید — دیگر هیچ‌وقت سراغ این قدم‌ها نمی‌روید.

---

## اگر پنل را نمی‌خواهید: ویرایش مستقیم روی گیت‌هاب

اگر قسمت ب برایتان زیادی است، لازم نیست انجامش دهید. محتوا را می‌توانید مستقیم روی خود گیت‌هاب ویرایش کنید:

۱. در مخزنتان روی فایل `content.json` کلیک کنید.
۲. آیکون **مداد** (بالا-راست) را بزنید تا ویرایشگر باز شود.
۳. متن داخل گیومه‌ها را عوض کنید (قانون‌های بخش ۵ راهنمای اصلی را رعایت کنید).
۴. پایین صفحه **Commit changes** را بزنید.
۵. تا یک دقیقه بعد سایت خودش به‌روز می‌شود.

برای عکس‌ها: وارد پوشهٔ `assets/images/projects/` شوید → **Add file → Upload files** → عکس را آپلود کنید → در `content.json` مسیرش را بنویسید.

این روش کمی خام‌تر از پنل است ولی هیچ تنظیمی نمی‌خواهد و همیشه در دسترس است.

---

## قسمت ج — وصل کردن دامنهٔ دلخواه (اختیاری)

اگر آدرس `اسم-کاربری.github.io/portfolio` برایتان کافی است، این قسمت را رد کنید. اگر دامنهٔ اختصاصی مثل `saeedmousavi.com` می‌خواهید:

۱. دامنه را بخرید (بخش «خرید دامنه» در `LAUNCH-GUIDE.md` را ببینید — همان توصیه‌ها).
۲. در پنل ثبت‌کنندهٔ دامنه، این رکوردها را اضافه کنید:

   | نوع | نام | مقدار |
   |---|---|---|
   | `A` | `@` | `185.199.108.153` |
   | `A` | `@` | `185.199.109.153` |
   | `A` | `@` | `185.199.110.153` |
   | `A` | `@` | `185.199.111.153` |
   | `CNAME` | `www` | `اسم-کاربری.github.io` |

۳. در گیت‌هاب: **Settings → Pages → Custom domain** → دامنه‌تان را بنویسید → **Save**.
۴. صبر کنید (تا ۴۸ ساعت، معمولاً یک ساعت)، بعد تیک **Enforce HTTPS** را بزنید.

> اگر دامنهٔ اختصاصی وصل کردید:
> - در OAuth App گیت‌هاب، **Homepage URL** را به دامنهٔ جدید عوض کنید.
> - در `content.json` مقدار `site.siteUrl` و در `robots.txt` و `sitemap.xml` آدرس را به دامنهٔ جدید عوض کنید.

---

## عیب‌یابی

**صفحهٔ سایت «404» می‌دهد**
معمولاً یعنی فایل‌ها یک سطح عمیق‌تر آپلود شده‌اند. در مخزن نگاه کنید: باید `index.html` را در سطح اول ببینید، نه داخل یک پوشهٔ `site`. اگر پوشهٔ `site` می‌بینید، محتویاتش را بیرون بکشید و دوباره آپلود کنید.

**سایت بالا می‌آید ولی می‌نویسد «content could not be loaded»**
تایپوی کوچک در `content.json`. متن فایل را در [jsonlint.com](https://jsonlint.com) بررسی کنید.

**در `/admin` دکمهٔ Login کار نمی‌کند یا پنجره باز می‌شود و می‌بندد**
سه چیز را چک کنید:
- در `admin/config.yml` مقدار `repo:` و `base_url:` درست پر شده باشد و فایل روی گیت‌هاب آپلود شده باشد.
- در OAuth App گیت‌هاب، **Authorization callback URL** دقیقاً برابر `آدرس-Worker/callback` باشد.
- در Cloudflare، هر دو متغیر `GITHUB_CLIENT_ID` و `GITHUB_CLIENT_SECRET` درست ست شده باشند.

**بعد از Publish، تغییر روی سایت نمی‌آید**
GitHub Pages یک دقیقه وقت می‌خواهد. صبر کنید، بعد در مرورگر `Ctrl + F5` بزنید.

**عکس روی سایت نمی‌آید ولی روی کامپیوترم درست بود**
تقریباً همیشه به‌خاطر فاصله یا حرف بزرگ در اسم فایل است. اسم فایل‌ها باید حروف کوچک انگلیسی و بدون فاصله باشد.

**فایل `.nojekyll` را یادم رفت آپلود کنم**
اگر بعضی چیزها (مخصوصاً پوشه‌هایی که با `_` شروع می‌شوند) درست سرو نمی‌شوند، `.nojekyll` را آپلود کنید. سایت فعلی پوشهٔ `_` ندارد ولی این فایل یک محافظ استاندارد است.

---

## خلاصهٔ چیزهایی که می‌سازید

| کجا | چه چیزی | چرا |
|---|---|---|
| GitHub | مخزن Public + Pages روشن | میزبانی رایگان سایت |
| GitHub | OAuth App | اجازهٔ ورود پنل |
| Cloudflare | یک Worker رایگان | واسط ورود بین پنل و گیت‌هاب |
| `admin/config.yml` | دو خط `repo` و `base_url` | وصل کردن پنل به این‌ها |

هزینهٔ کل: **صفر** (به‌جز دامنهٔ اختیاری). هر جا گیر کردید، بگویید تا کمکتان کنم.
